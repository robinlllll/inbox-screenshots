---
podcast: "硅谷101"
episode: "E228｜谷歌TPU能撼动英伟达吗？前TPU工程师首次揭秘"
link: https://podwise.ai/dashboard/episodes/7501932
publish-time: "2026-03-13"
save-time: "2026-03-13"
---
# Summary

本期《硅谷 101》探讨了谷歌的 TPU 芯片及其在人工智能领域的潜力。前谷歌 TPU 工程师 Henry Zhu 详细介绍了 TPU 与英伟达 GPU 的架构差异，TPU 专为机器学习中的矩阵计算而优化，通过流水线作业提高计算效率。讨论深入到 TPU 的硬件、软件和生产环节，包括与 Anthropic 和 Meta 的合作，以及 XLA 编译器在 TPU 生态中的作用。Henry 强调，在特定条件下，TPU 完全可以挑战 GPU 的垄断地位，尤其是在大规模部署和模型相对稳定的情况下，TPU 在成本和性能上更具优势。同时，他也指出了 TPU 在供应链、软件生态和适应新算法范式方面面临的挑战。

# Takeaways

* 谷歌的 TPU 在大规模部署和模型相对固定的情况下，通过软硬件协同优化，能够实现比 GPU 更优的成本效益。
* TPU 的核心优势在于其系统级优化能力和软硬件协同能力，这使得它在处理大规模、静态形态的模型时，能够有效控制成本。
* TPU 与 GPU 的根本区别在于，前者是针对机器学习定制的加速器，后者则更多是通用的并行处理器。
* 谷歌 TPU 的成功很大程度上归功于其对 Transformer 架构的早期押注和深度优化，使其在特定工作负载下性能卓越。
* 尽管 TPU 在某些方面性能优异，但其软件生态（如 XLA）的复杂性和高门槛限制了其在开发者中的普及。
* HBM 的供应量和质量直接影响 TPU 的性能上限，而英伟达在 HBM 市场上占据主导地位。
* Grok 通过专注于低延迟推理和为特定应用场景（如 Agent）定制芯片，在推理芯片市场中找到了差异化定位。
* TPU 的设计理念是牺牲硬件的通用性，转而依赖软件来处理复杂性，从而实现更高的计算效率。
* 谷歌 TPU 集群采用芯片间直接通信和光学交换机等技术，降低了数据中心建设成本，从而在推理成本上优于 GPU 集群。
* 尽管 TPU 在某些领域具有优势，但如果未来大模型的训练范式发生根本性变化，其专用性可能会成为劣势。

# Q & A

**Q: 谷歌的 TPU 能挑战英伟达在 GPU 领域的垄断地位吗？在哪些限定条件下，TPU 可以挑战 GPU？**

A: 没有绝对的答案。在某些限定条件下，TPU 完全可以挑战 GPU。大规模部署是其中一个条件，因为 TPU 主打走量，不擅长针对单用户，比如做 agent 就不太适合，因为延迟较高。TPU 必须在一个非常大的吞吐量下，比如有很多用户同时调用接口，才能把成本分摊开来，得到一个很好的吞吐量。在这种情况下，大规模部署，模型相对比较稳定，不需要很多变动，整体成本相比 GPU 有很大优势。如果模型相对固定，不需要很多改变，形态也比较静态，TPU 非常适合大规模部署。如果你已经训练好一套模型，只需要做 inference，TPU 的 system level 优化能力，软硬件协作能力能帮助你把成本控制在一个非常好的范围内，这是 TPU 最大的优势，但前提是必须是非常大的用户群，不适合本地部署，很适合在云上用，适合 Gemini、ChatGPT、Claude 这种每天都有数百万用户的应用，这样才能把成本分摊给每个 user。

---

**Q: 训练一个 Gemini 模型，用英伟达的 GPU 和谷歌的 TPU 相比，谁更省钱？**

A: 我的理解是，针对自家定制的大模型，比如谷歌的 Gemini，将来如果谷歌给其他大公司 Frontier 大模型公司定制的话，性价比是更高的，性价比我指的是 At TCL, the total cost of ownership，就是它的成本会更加有优势。当你知道你的 workload 是什么的时候，你就可以根据你的 workload 去做一些不管是物理，芯片层面的一些定制，或者说在软件层面去定制。虽然说它可能有点黑盒的感觉，但是就是我知道相当于给出了你所有的 assumption, 你所有的已知条件都确定下来，那我觉得 TPU 在现实生活中，在现实条件下，它的训练效率还是 TCU 都是会比 GPU 更加强大的。它的 utilization rate 是更好的原因，第一是它的一个 flops，它的 flops 我们指的就是单位时间内它做多少次浮点式运算。因为 TPU 它里面主要的架构就是矩阵计算，所以它软件和硬件可以保证它每次，每个时间，每个计算单元它都有活在做。

---

**Q: TPU 和 GPU 在模型训练上的架构有什么不同？各自的优势和缺点是什么？**

A: TPU 和 GPU 本身它的架构是完全不一样的。GPU 大家可能更加了解，因为它是最开始做游戏的显卡，所以它里面会用到一个数叫 SimT，它是一个 SimT 的架构，Single Instruction Multiple Threading，它是个多线程单一指令的一个架构，所以可以理解成一个厨房里面同时安排着很多很多个大厨，他们每个人都有自己独立的思考能力，然后独立去从冰箱里把东西拿出来，然后做完菜，然后最后上菜，他们可以独立完成这样一件事情，所以你安排很多很多大厨的话，这样的话，并行计算能力就会非常非常的强。TPU 和 GPU 最大的区别就是 TPU 是一个针对机器学习的一个加速器。我们知道机器学习任何的算法，包括最开始的 CNN 到现在的 Attention 到到 Transformer, 到未来的架构，里面的核心就是矩阵计算。TPU 的话就是针对这个矩阵计算专门做了一个定制的加速器。所以你可以理解成做饭的话，我们是一个流水线，我们知道今天要做哪道菜，TPU 的话相当于是把不用安排那么多的大厨，但是他会把大厨安排在每一个步骤上，会把每一个步骤都告诉你具体做哪个步骤的火，然后你做完之后直接把你做完的，比如说第一个人会从冰箱里把菜给取出来，第二个人直接把第一个人做完的部分的中间菜的结果做加工传到第三个人。

---

**Q: 谷歌的 Gemini 模型训练出来跟 TPU 的关系有多大？**

A: 关系是很大的。算法当然很重要，因为我觉得现在就是训练，特别是 pre-training, 它 capex 很大，然后它算法我觉得 D-Mine 里面有非常非常前沿的算法。TPU 能帮你去做的一件事情，当你有一个很好的算法之后，然后你提前跟 TPU 子沟通好，我是怎么样的一个算法，我是怎么样的一个 workload，我给你去定制这样的一个加速器，这样的话我能帮助你一个更短的时间，更快的迭代，去测试这样一套算法。这是一个很重要的事情，就是如果说你的训练效率很差的话，你需要花更长的周期去验证一套算法是否能 work, 但 TPU 如果能把你的时间打下来之后的话，迭代速度就会更快一些。

---

**Q: 谷歌 TPU 的产量是多少？**

A: 具体数据我不是非常清楚，但我觉得也是在一个慢慢的爬坡的过程当中，谷歌 TPU V7 之前的话一直是有一个产能的问题，我觉得也是一个很多因素导致的，因为毕竟 V7 之前我们一直没有一个对外的一个生态，所以我们更多是针对内部的一个 deploy 和使用，所以我们没有办法和 Broadcom, TSMC 或者刚才所说的那几家 HBM 的厂商去锁定一个很大的一个订单。因为产能都是提前一年或两年去锁定的。当你没有那么大的客户或没有那么大的需求的话，就是你临时想去调整是比较困难的一件事情。还有一点就是 CoreOS 是 TSMC 的一个 capacity, 我们可以理解成我们现在的芯片都是跟以前完全不一样，因为我们现在都是做一个 code design, 我们的 HBM 内存芯片和计算芯片是两块独立的芯片，通过一个 2.5D Stacking 的一个封装，把它封装成一个集成芯片。这个的话，TPU，谷歌自己做不了，Broadcom 也做不了，它只能依赖于 TSMC。所以既然 Samsung 给你分配多少的产能，你就能用你达成多少的产能。

---

**Q: Anthropic 向谷歌采购 100 万颗 TPU，这个订单量大吗？Anthropic 和谷歌之间是什么关系？**

A: Anthropic 这个订单确实是挺大的一个订单。第一有很多个因素吧。第一，我觉得 Anthropic 和谷歌是一个相对于内循环，因为 Anthropic 很多投资方也是谷歌的。所以我觉得 Anthropic 和谷歌是一个深度合作的一个关系。相当于如果说是 Meta 或者其他的公司的话，我不确定它的成本，它的 TCO 到底能不能压得下来。然后第二点的话，我觉得就是 Anthropic 它的工程师的技术能力还是非常非常强的。所以他们能去用 TPU 来部署他们自己的自家的模型。为什么 TPU 的部署在一般 external third party customer 上那么的难。但我觉得 Anthropic 目前来讲是有这样一个谷歌的生态的。

---

**Q: 谷歌的 TPU 在生产环节上，有哪些瓶颈？**

A: 产能的瓶颈主要有两块。一块是 Cohost 跟 TSMC 这块是在封装上的一个产能，然后还有一块就是 HBM，它是高带宽的内存供应，然后这个可能也是一个瓶颈，就是有三大垄断巨头去把持。还有一点就是两律。两律的话就意味着当你 TSMC 把一个 Wafer 生产出来之后，它上面这一块 Wafer 上有多少是合格的芯片。因为 TPU 和 TPU 它的制造理念是不太一样的。TPU 它更多是主打一个通信，它是主打一个芯片与芯片之间的通信。然后这样的话导致它的中间的失败率会比 TPU 更高一些。然后一旦失败了，因为它保证整个系统里每张芯片都是性能大致是一样的，如果层次不齐的话，它整个系统跑下来的效率就不会有那么的高，但 GPU 可能就不存在太大这样的问题，因为我们都知道 GPU 有自己阉割的版本，就是 H100A100，所以一旦良率不好的话，它可以往下降级，但是 TPU 不可以，因为是定制的芯片，价格都会不太一样，不是通用的芯片，所以一旦你良率不行的话，就这一块芯片相当于报废了，所以相当于你产能就没办法一下子提上来。

---

**Q: 谷歌的 XLA 编译器是什么？它和英伟达的 CUDA 有什么区别？**

A: XLA 也是谷歌的一个，你可以理解成黑盒，也可以理解成是一个 secret sauce。XLA 和 CUDA 最大的区别，XLA 是一个静态的编译器，然后静态编译器指的就是当你的 workload 是已知的或者给定的话，它可以在一个全局的 TPU pod 的一个 system level, 它可以把它做一个全局的优化。优化分很多层面，我知道就是他们会做很多内部的算子融合，比如说你一个 kernel 里面有很多计算，然后你还会把一些计算合并到一个计算单元，这样的话，你能更好地去利用你的 Systemic Array 里面的矩阵计算的利用率，然后你中间的中心态结果你不用反复地存进内存，存出来。XLA 会帮你自动做这样的优化，然后它还会做很多的一些内存的管理，怎么去更好地去分布你的内存。它知道 TPU 喜欢怎么从内存里面去把数给读出来。这些东西都是跟硬件架构和一些具体的细节是非常非常相辅相成的。所以 XLA 会通过一个黑盒帮你去优化这样的一件事情，但问题就是这个事情你是没办法去很好去 debug 和去控制。

---

**Q: 如果使用谷歌的 TPU 训练模型时出现问题，该如何解决？**

A: XLA 是黑客，但是它里面有很多很多的帮助你去 debug 的一些工具，而且 function。但它问题就是你需要对硬件是有一些了解，你才能更好地去分析它。因为它 XLA 它会把很多算法做融合，然后内存做管理，这样的话它是一个 graph，它不是一个单个的计算，它是一个链条，一个图表，所以相当于你这样 debug 起来，它对工程师的要求会更，它是可以 debug，但对工程师的要求会更高一些。一旦发现 XLA 的 bug，你就必须让 Google 的工程师来去帮忙去分析去解决。如果真的是个 bug 需要去解决，就外部的开发者很难独立去处理一个或 fix 一个 bug, 不像 Kuda 的话是有一个很好的生态你可以去。

---

**Q: 如果大模型的训练范式发生变化，会对 TPU 的销量产生什么影响？**

A: 不完全准确，就 Transformer 它核心还是矩阵计算。如果出现了类似于不同于 Transformer 的新的架构，那 TPU 它就不一定有这个 GPU 它的表现好。如果是在模型层面，比如说有一些这种 MOE 的一些新的方法的微调，但它还是基于整个 Transformer 的，这种你觉得 TPU 未来它还是会持续的占优势吗？包括大家现在很热的说，我要用这个 RL 的方式去做 agent 的训练，或者去做模型的训练。就是 RL 对 TPU 的影响会大吗？肯定是会是不一样的 workload 的，肯定是会有影响，就是我觉得像 TPU 的一个策略就是把芯片变得更加的通用，我也是在往这样一个方向去发展，相当于它矩阵单元，它可以处理各种 workload 的矩阵计算，然后它也有专门负责去处理一些稀疏的矩阵计算的一些单元，它可以理解成把很多硬件上的一些计算和内存模块化，来适应将来不同的那个方式。所以从某种意义上来讲也是一个向通用性的一些妥协。

---

**Q: 谷歌和 Meta 之间的 TPU 采购协议，核心目的是什么？**

A: 我也有了解，当然具体的细节还没有没有公开嘛。所有的 Meta 都是用 Pytorch，大家应该是众所周知。所以 Pytorch，刚才我有提到它跟 TPU 的生态其实不是特别的兼容，所以它很难像 Anthropic 那样做一个深度的对 TPU 整个软硬件生态的使用。目前来讲，我觉得它可能更多是依托于谷歌云，谷歌 Cloud, 提供更多算力，因为它今年它其实股价不是那么好的原因，也是一个 KPAC 实在是太大了，它对特别预训链这一块的成本支出实在是需求非常非常的大。他已经把市面上所有能买来的算力都买来了，去做这样一件事情。所以我觉得 TPU 目前来讲也是去帮他去 offload 他这一方面的一些压力。软件态的话，Pytorch 很早之前就跟 TPU，包括 Fair 那个团队，跟 TPU 都有接洽，就说能不能在 TPU 上更好地去支持 Pytorch, 这样的话我们也更好地去做一些 research 的开发。但目前来讲，我觉得谷歌也是有在去做一些改变了。离职之前也知道有很多组在做 PyTouch 和 XLA 的结合，包括在 XLA 和 TPU 上支持很多 Native 的一些 PyTouch 的一些 Library, 一些并行的 Library 和一些算子。因为现在 PyTouch 的算子实在是太多了，它可能有好几千个算子，如果你不在硬件上原生的去支持这些算子的话，你的性能表现就会比较差一些。

---

**Q: 直接使用谷歌云有什么问题？**

A: 问题就是直接用谷歌云的话相当于你把，你相当于是拖管了，你没办法做很多底层的去控制。如果你想去挖掘每一分的性能的话，你从谷歌云就没办法去做这样的事情。就是会有浪费。如果说你结合的非常好的话，你能几乎满状态的达到一个 peak memory bandwidth, 但是如果用谷歌运营来跑的话，你很有可能用到只有 50%,60% 它的 utilization rate, 但是你还是要付同样的钱。

---

**Q: 谷歌的 TPU 在推理成本上比 GPU 更高吗？**

A: 推理成本上确实是这样。我觉得 Google 它现在的推理芯片成本确实会比 GPU 要高不少。它的原因就是刚刚所说它是一个集群的一个推理。它的 TCO 就能打得下来。GPU 的集群，它用的是一种 NVLink 和 NVSwitch 这样的一种通信协议。这个其实很烧钱。它是一个你可以理解成是一种 Infrastructure 的一种 Text。所以你需要跟很多不同厂商去买这种交换机，然后部署在你的数据中心当中，这是一个很大的成本开支。谷歌因为它用了它不一样的一个拓扑架构，它用了一个是芯片与芯片之间直接通信，它用的是个桶，它不用交换机，然后它只有在某些节点上用一些光学交换机，它也比较少，然后实现了同样的一个通信的一个效果，所以在成本支出上就是会比 GPU 要好很多。

---

**Q: 英伟达和谷歌在搭建数据中心时，主要成本分别是什么？**

A: NVIDIA 的主要成本是交换机。他们搭的整个的这个芯片机群里面还要铺很多的铜，是不是他们都是用这种铜的这个线来连接起来的。谷歌的主要成本是一脸是一块吧，其实跟英伟达也差不太多，它主要成本也是它的一些 Surges, 我们会跟 Broadcom 深度合作一些这种 Surges, 就相当于是把信号从一个芯片准确无误的传输到另外一个芯片，因为相比 GPU 的话，TPU 它更多依赖于 Surges 的一个稳定性，所以它的那块资本支出还是很高的。

---

**Q: 博通在谷歌 TPU 供应链中扮演什么角色？**

A: 博通主要的核心作用它是做很多的通信，ICI, Interchip Connect，然后它会做很多的后端。我们芯片的话，我们 TPU 团队主要设计它的前端，比如说我们相当于是把一张图纸给打印出来，然后 BlockCon 会把每个芯片与芯片之间物理上去连接起来，然后最后交付给 TSMC 在物理上去生产出来，所以 BlockCon 更多是做一个信号的连接，怎么去布局这样一个突破的一个网络，所以 BlockCon 可能是在这一边。

---

**Q: 谷歌 TPU 未来发展面临的最大挑战是什么？**

A: 我担忧的一点就是这一套架构它的 scaleability 到底有多强，如果说将来有一个完全不同的一个方式，完全不同的一个模型，你没办法在 TPU 上做这样的去优化。然后 GPU 上可以，这样的话你定制芯片因为 TPU 团队跟 GPU 是不是一个量级的，你没办法做一个快速的迭代，这样的话你的性能上差距可能就会被拉大。所以我觉得这个很难去评判现在，我觉得就是可能是我对 TPU 的一个担忧吧，就是万一将来有不同的范式，它怎么去。

---

**Q: 谷歌 TPU 的发展历史是怎样的？**

A: 最开始 TPU 的话主要是针对内部 CNN 追打模型的一个加速器，然后最开始第一代芯片它只是一款 Inference 芯片，因为 Inference 相比 Train 更好做一些，所以 Inference 相当于是一个 testing chip，作为一个入口，我们先把这个它里面的架构相对比较简单，它就是一个矩阵计算机加内存，没有其他更多的一些模块，然后最开始的初衷就是大家发现我们内部有很多的 online 的 recommendation system, 但它的 inference 都是用 CPU 来做 inference。大家都知道 CPU 是什么样的问题。就是对它的并行效果是非常非常差的。然后那时候也没办法用 GPU, 因为那时候 GPU 它还没有加入它的那个矩阵计算单元，所以它更多是一个做显卡，做一个 pixel。所以那时候没有更好用的芯片了。所以谷歌说为什么不自己开发一款只做矩阵计算的模型？所以最开始就是最开始初衷应该是 Jeff Ding 和后来图林讲，获得者 David Patterson 也是深度参与了第一代模型的架构，所以设计出来性能非常非常的好，我们有了第二代芯片。第二代芯片它就是一个非常旗舰的做 training，做训练的模型，所以包括我们后来知道的 AlphaGo，包括 Palm，包括 Bart，包括早期的 Early Transformer, 包括后来 Transformer，都是用了这一套架构去做的训练。

---

**Q: Grok 公司是如何在推理芯片领域精准地踩到每一个时代红利点的？**

A: 目前他们被英伟达收购是踩准了每一个时代的红利，踩准了每一个很好的时间点。第一个时间点就是 Inference，然后第二个时间点是 ASIC, 第三个时间点就是今年是 Agen 的元年嘛，相当于有很多智能体的爆发。然后 Grok 最好的一个应用 case 就是 Agent 智能体。因为 Agent 智能体的话，它对延迟的要求是非常非常高的。如果说你的延迟做得很差的话，你整个帮你去做这一个任务的一个链条就会被拉到无限长。这是对于单用户来讲的话是一个非常痛苦的一件事情。所以 Grok 就是能很好地去解决或者说解决这样的一个问题。

---

**Q: Grok 的芯片优势是什么？**

A: Grok 的芯片，因为 Johnson Ross 最开始是 TPU 的 compiler team, 它是 TPU compiler 那边的一个 Lithuania founder, 这相当于它是带了一套它非常成熟的 TPU 的 compiler XLA 的一些经验去扣放了 Grok, 所以 Grok 你可以理解成它是一家 compiler 的公司而不是一家芯片公司，因为它的芯片更多是为它的软件 compiler 服务的。它的硬件可能比 TPU 更加的单一或者说没那么的智能一些。它的 compiler 在某种程度上决定了一切。它 LPU 里面每一个时间点，每一个计算单元里面具体做哪些事情，它可以精准到每一个 cycle，都是用 compiler 去确定好的一个非常确定性非常高的一个事情。

---

**Q: 未来的推理芯片市场会呈现怎样的格局？**

A: 自从去年 DeepSeek 出来之后，大家一下子发现就是在推理端就是如果你成本打下来之后的话，端测然后本地的部署，它的推理的需求是非常非常大的。所以这样的话，我觉得就是有很多不同 Layers 的市场和不同 Layers 的那个的指标的需求。当然，谷歌和 TPU 肯定会占据最高层，就是最大规模那些部署的，包括运营啊，包括那些大模型的推理。然后中间和下面的一层的话，我觉得会有更多的玩家，更多的参与者进来。然后这样的话，相当于 Grok 这块芯片，其实它如果是一个非常大参数量的模型的话，它其实成本是挺高的，我觉得。对，如果说是一个小规模部署的话， 它成本是可以控制的。

# Outlines

## 谷歌 TPU 挑战英伟达 GPU 垄断地位：前谷歌工程师深度解析
[(00:00)](https://podwise.ai/dashboard/episodes/7501932?locate=0)
2025 年谷歌的 Gemini 3 使用 TPU 而非英伟达 GPU 进行训练，TPU 驱动了谷歌的核心产品。苹果、Anthropic 和 Meta 等公司也纷纷采用 TPU 训练模型。在某些芯片条件下，TPU 完全可以挑战 GPU，未来两者将并存。本期硅谷 101 将讨论谷歌的秘密武器 TPU，并邀请前谷歌 TPU 工程师 Henry 分享他对 TPU 的理解。

---

## TPU 与 GPU 架构对比：流水线式矩阵计算加速机器学习
[(01:15)](https://podwise.ai/dashboard/episodes/7501932?locate=75)
Henry Zhu 是谷歌前 TPU 工程师，曾深度参与三代 TPU 的研发。TPU 是一个上下游产业链复杂的产品，涉及软件、硬件和代工。TPU 和 GPU 的架构完全不同。GPU 最初用于游戏显卡，采用 SimT 架构，类似于多个独立大厨并行工作。TPU 则是针对机器学习的加速器，核心是矩阵计算，采用流水线式作业，将大厨安排在每个步骤上，减少调度和调控，提高计算单元的使用率。

---

## TPU 在模型训练上的优势：弥补 GPU 内存利用率不足
[(05:16)](https://podwise.ai/dashboard/episodes/7501932?locate=316)
GPU 的 CMD 架构在数据搬运过程中存在闲置期，导致矩阵计算利用率不高。TPU 弥补了这一缺点，通过软硬件协同效应，保证满负荷运行，利用本地缓存或跑满带宽，达到内存利用率的峰值。针对自家定制的大模型，谷歌的 TPU 性价比更高，成本更具优势。TPU 的软件和硬件可以保证每个计算单元都有活在做，不会让硬件闲下来。

---

## TPU 的硬件瓶颈：HBM 供应、封装产能与良率挑战
[(09:02)](https://podwise.ai/dashboard/episodes/7501932?locate=542)
Ironwood 在 flops 和 memory bandwidth 上都有巨大提升。HBM 市场被 SK Hynix、三星和 Micron 三家公司垄断，英伟达是 HBM 的最大客户，TPU 是次要客户，因此 TPU 获得 HBM 订单需要良好的合作关系。TPU V7 之前存在产能问题，因为没有对外生态，难以锁定大订单。芯片制造采用 code design，HBM 内存芯片和计算芯片通过 2.5D Stacking 封装，依赖于 TSMC 的产能分配。

---

## TPU 良率与 Anthropic 订单：内循环生态与技术能力
[(12:14)](https://podwise.ai/dashboard/episodes/7501932?locate=734)
TPU 的良率低于 GPU，因为 TPU 更注重芯片间的通信，一旦失败则芯片报废。Anthropic 采购 100 万 TPU 的订单很大，但 Anthropic 与谷歌存在内循环关系，投资方也有重叠，是深度合作关系。Anthropic 的工程师技术能力强，能用 TPU 部署自家的模型。TPU 的部署在外部第三方客户上比较困难。

---

## XLA 编译器：谷歌 TPU 的黑盒与 Anthropic 的软件生态
[(15:37)](https://podwise.ai/dashboard/episodes/7501932?locate=937)
Anthropic 与谷歌的软件生态有关，因为其许多工程师来自谷歌，了解 TPU 上的软件生态部署。XLA 是谷歌的黑盒，也是 secret sauce，它是一个静态编译器，可以在全局 TPU pod 的 system level 做全局优化，包括算子融合和内存管理。开发者可以使用 PyTorch, JAX 和 TensorFlow 等语言，通过 XLA 转化成 TPU 的指令。Anthropic 可以使用 TPU 训练模型，但 XLA 出现 bug 需要谷歌工程师解决。

---

## 苹果是谷歌 TPU 最大客户：软件债迁移与 MOE 优化
[(19:24)](https://podwise.ai/dashboard/episodes/7501932?locate=1164)
谷歌有软件组专门对接不同客户，包括苹果、Meta 和 Anthropic。苹果是谷歌 TPU 最大的团队，将谷歌的一整套带过去，用 TPU 训练大模型。使用 TPU 需要将原来的软件债全部迁移到 JAXXLA 上，才能利用 TPU 的性能和 TCU。Gemini 模型训练与 TPU 关系很大，TPU 可以帮助在更短的时间内更快地迭代和测试算法。TPU 针对 MOE 的优化，通过 3D Taurus 和 OCS 光铅交换机解决了 MOE 的痛点。

---

## TPU 迭代周期：预测模型变化与 Transformer 架构优势
[(23:45)](https://podwise.ai/dashboard/episodes/7501932?locate=1425)
芯片公司都在预测未来市场和模型的变化，TPU 的迭代是以年来计算的，设计到量产最快也要两年到三年。V7 在设计上压对了方向，物理参数上非常接近 GB200。TPU 的主要 workload 是内部的推荐和 ranking system 算法，没有想到 GPT 对算力有如此大的需求。谷歌很早就知道 Transformer 架构，芯片组也针对这套架构做优化，TPU 可以理解成一个针对于大模型预训练或推理的 ASIC。

---

## TPU 的挑战：通用性与未来模型范式变化
[(27:54)](https://podwise.ai/dashboard/episodes/7501932?locate=1674)
TPU 的优势在于对 Transformer 架构的先发优势，但 ASIC 的通用性不如 GPU，模型迭代周期短，一旦芯片架构固定，难以适应新的算法。V7 与 GPU BlackHawk 旗鼓相当，但未来若出现完全不同的模型，TPU 难以优化。即使 Transformer 架构不变，MOE 等新方法也会对 TPU 产生影响。TPU 的策略是将芯片变得更加通用，模块化计算和内存模块，以适应未来的方式。

---

## 谷歌与 Meta 合作：PyTorch 中间层与谷歌云托管服务
[(31:41)](https://podwise.ai/dashboard/episodes/7501932?locate=1901)
谷歌在软件层面发力，让中间软件适应更多开发者。谷歌向 Meta 提供 TPU 托管服务，将 PyTorch 变成 TPU 的中间层。Meta 使用 Pytorch，但它与 TPU 的生态不兼容，难以像 Anthropic 那样深度使用 TPU 的软硬件生态。Meta 依托于谷歌云，TPU 可以帮其 offload 压力。谷歌也在做 PyTouch 和 XLA 的结合，在 XLA 和 TPU 上支持 Native 的 PyTouch Library。

---

## TPU 的限定条件：大规模部署与模型稳定性
[(35:59)](https://podwise.ai/dashboard/episodes/7501932?locate=2159)
直接使用谷歌云无法做底层控制，会浪费性能。Meta 与 Google 的合作是在谷歌云上跑。Anthropic 直接从 Blocker 购买 TPU 计价的 Rack，其他公司都在谷歌云上跑。谷歌工程师在 JAX 和 XLA 方面经验丰富，能更好地迁移过去。谷歌的 API 接口成本最初只有 OpenAI 和 Anthropic 的十分之一，Anthropic 的 API 接口下降 67%，归功于使用谷歌 TPU 训练。谷歌的推理芯片成本比 GPU 高，但 TCO 能打下来。

---

## TPU 挑战 GPU 垄断：大规模部署与软硬件协同优势
[(42:52)](https://podwise.ai/dashboard/episodes/7501932?locate=2572)
在某些限定条件下，TPU 完全可以挑战 GPU。TPU 擅长走量，不擅长针对单用户，必须在大吞吐量下才能分摊成本。模型相对固定，不需要很多变动，TPU 的整体成本相比 GPU 有很大优势。TPU 的 system level 优化能力和软硬件协作能力能帮助模型把成本控制在很好的范围内。TPU 必须是一个非常大的用户在用，适合在云上使用。

---

## TPU 发展历程：从 Inference 芯片到 AlphaGo 的突破
[(49:47)](https://podwise.ai/dashboard/episodes/7501932?locate=2987)
TPU 最初是针对内部 CNN 追打模型的加速器，第一代芯片只做 Inference。最初的初衷是解决内部 online recommendation system 用 CPU 做 inference 并行效果差的问题。Jeff Ding 和 David Patterson 深度参与了第一代模型的架构设计。第二代芯片是旗舰的训练模型，AlphaGo 使用了这一套架构。硬件先行，软件滞后，硬件理论上不错，但产能和软件协同效应跟不上。

---

## TPU 与 Grok：不同时代的红利与市场差异化
[(55:17)](https://podwise.ai/dashboard/episodes/7501932?locate=3317)
Jonathan Rose 在内部演示中发现 GPU 成本过高，促使谷歌研究 TPU。AlphaGo 击败围棋冠军李世石时，使用了 4 张 TPU。第五代到第六代，TPU 重心转向 LLM。Grok 被英伟达收购，踩准了 Inference、ASIC 和 Agent 的红利。Grok 的芯片是为软件 compiler 服务的，硬件更加单一，compiler 决定一切。Grok 适合 Agent 智能体，对延迟要求非常高。

---

## Agent 应用与推理芯片：百花齐放与打破垄断
[(1:01:24)](https://podwise.ai/dashboard/episodes/7501932?locate=3684)
Grok 与 IBM 签订数据中心合同，针对本地人使用，提供私有语音。未来 Agent 应用中，推理芯片会是一个百花齐放的格局，英伟达的 GPU 为主导。TPU 占据最高层，中间和下面一层会有更多参与者。TPU 可能是一个很好的挑战者，需要把软件生态像 CUDA 一样搭建起来，需要同行的激励和鞭策。

---

## TPU 的未来：市场份额、模型能力与产业链影响
[(1:04:27)](https://podwise.ai/dashboard/episodes/7501932?locate=3867)
华尔街关注 TPU 能抢走英伟达多少市场份额。大模型厂商如 Anthropic、Meta、XAI 和 OpenAI 都可能与谷歌进行 TPU 合作。TPU 的供应链环节，包括 HBM 和液冷，以及数据中心的布局，会影响到产业链中的各个环节和厂商。感谢 Henry 分享技术细节，为决策提供参考价值。

# Keywords

|Keywords|Explanation|
|---|---|
|Gemini 3|谷歌正在研发的下一代 AI 模型，预计在 2025 年发布，并可能在 AI 性能上超越其他模型。谷歌希望通过 TPU 来训练 Gemini 3，挑战英伟达 GPU 在 AI 训练领域的地位。|
|TPU (Tensor Processing Unit)|谷歌专门为机器学习定制的加速器芯片，特别擅长矩阵计算。与英伟达的 GPU 相比，TPU 在特定工作负载下，如谷歌自家的 Gemini 模型，能提供更高的性价比和训练效率。|
|AlphaFold 2|谷歌 DeepMind 开发的人工智能程序，能够根据蛋白质的氨基酸序列预测其三维结构。TPU 驱动了 AlphaFold 2 的运行，显示了 TPU 在解决复杂科学问题上的能力。|
|Claude|Anthropic 公司开发的大型语言模型，是 OpenAI 的 ChatGPT 的竞争对手之一。Anthropic 计划使用价值数百亿美元的 TPU 来训练下一代 Claude 模型。|
|Lama|Meta 公司开发的大型语言模型，Meta 与谷歌签订了数十亿美元的协议，租用 TPU 来运行 Lama 模型。|
|GB200/H100|英伟达（NVIDIA）生产的高性能 GPU 型号，常用于 AI 模型的训练和推理。GB200 和 H100 是目前市场上领先的 AI 芯片，但面临来自谷歌 TPU 的竞争。|
|XLA (Accelerated Linear Algebra)|谷歌的静态编译器，用于优化机器学习工作负载在 TPU 上的执行。XLA 能够对全局 TPU pod 系统进行优化，包括算子融合和内存管理，但开发者难以 debug 和控制。|
|CUDA|英伟达（NVIDIA）的并行计算平台和编程模型，为 GPU 上的通用计算提供支持。CUDA 拥有庞大的开发者生态系统，方便工程师解决 bug 和优化性能。|
|Transformer|一种神经网络架构，在自然语言处理领域取得了巨大成功，是现代大型语言模型的基础。谷歌发明了 Transformer 架构，TPU 从诞生之初就针对 Transformer 进行了优化。|
|MOE (Mixture of Experts)|一种模型架构，通过组合多个 “专家” 模型来提高整体性能。TPU 在 V4 版本中通过引入 3D Torus 网络和 OCS 光铅交换机，解决了 MOE 模型在 TPU 上运行效率低的问题。|
|Broadcom|博通公司，是谷歌 TPU 芯片的关键代工合作伙伴，负责芯片间的通信设计和后端制造。谷歌与博通的合作关系对 TPU 的产能和成本控制至关重要。|
|TSMC|台积电（Taiwan Semiconductor Manufacturing Company），全球最大的半导体代工厂。TSMC 负责 TPU 芯片的封装和量产，其产能分配直接影响 TPU 的供应量。|
|HBM (High Bandwidth Memory)|高带宽内存，一种高性能的内存技术，用于加速数据传输。HBM 的供应量和性能对 AI 芯片的性能至关重要，英伟达目前在 HBM 的采购上占据优势地位。|
|Grok|一家专注于低延迟推理芯片的公司，后被英伟达收购。Grok 的芯片设计和编译器技术使其在 Agent 智能体等对延迟要求高的应用场景中具有优势。|

# Highlights

- [(08:34)](https://podwise.ai/dashboard/episodes/7501932?locate=514) 当你知道你的 workload 是什么的时候，你就可以根据你的 workload 去做一些不管是物理，芯片层面的一些定制，或者说在软件层面去定制。
- [(28:09)](https://podwise.ai/dashboard/episodes/7501932?locate=1689) 本质上 ASIC 和 GPU 它就是一个是通用性好，一个是通用性不好，就是说即使我们没有一手的信息。
- [(40:44)](https://podwise.ai/dashboard/episodes/7501932?locate=2444) 未来几年的方向可能就是 HBM 能决定你的上限。如果你买不到好的 HBM 的话，那你的清理效率就会大打折扣。
- [(42:20)](https://podwise.ai/dashboard/episodes/7501932?locate=2540) 没有绝对的答案，但我的结论是在某些限定的条件下，如果一旦满足了这些限定条件，我觉得 TPU 是完全可以挑战 GPU 的。
- [(1:03:41)](https://podwise.ai/dashboard/episodes/7501932?locate=3821) 我觉得这个生态是一个非常健康的生态，有定制的环节，也有通用的环节，有通用的场景，也有定制的场景，也有锤类的场景，所以我觉得将来是百花齐放的一个格局。

# Transcript

**泓君:**
[(00:00)](https://podwise.ai/dashboard/episodes/7501932?locate=0)
2025 年，当谷歌的 Gemini 3 登顶 AI 性能排行榜的时候，训练它的不是英伟达的 GPU, 而是谷歌自己的芯片，TPU。从搜索、翻译到地图，甚至是 Alpha Fold 2, TPU 它驱动了谷歌几乎所有的核心产品，而如今它正在向整个的 AI 产业渗透。2024 年，苹果发表论文，Apple Intelligence 全部使用 TPU 训练。2025 年，Anthropic 拿下 100 万颗 TPU, 价值数百亿美元，用来训练下一代的 Claude。2026 年刚开年，Meta 就签下了数十亿美元的协议，租 TPU 来跑 Lama。过去我们说整个市场都认为未来 AI 芯片的机会在于推理芯片，英伟达在训练芯片的地位是难以撼动的。但是我们看到现在在越来越多的顶级模型公司中，谷歌的 TPU 正在成为英伟达的替代方案。你觉得现在谷歌的 TPU 可以挑战英伟达在 GPU 的垄断地位吗？

**Henry Zhu:**
[(01:05)](https://podwise.ai/dashboard/episodes/7501932?locate=65)
在某些芯片的条件下，TPU 是完全可以挑战 GPU 的。很多人都在讨论 TPU 和 GPU 之间的区别，我熟悉了。未来肯定是两者并存的。

**泓君:**
[(01:15)](https://podwise.ai/dashboard/episodes/7501932?locate=75)
哈喽，大家好，欢迎收看硅谷 101。我是泓君。今天我们来聊一聊谷歌的秘密武器 TPU。那今天跟我在一起的是谷歌前 TPU 的工程师 Henry。哈喽，Henry，你好。

**Henry Zhu:**
[(01:31)](https://podwise.ai/dashboard/episodes/7501932?locate=91)
Hello, 洪君， 你好。首先给大家自我介绍一下。我的名字叫 Henry。很开心硅谷 101 的邀请，然后我也是你们忠实的听众粉丝。我之前上一份工作是在谷歌的 TPU 组，然后我也深度参与了三代 TPU 的研发。像最近发布的 Ironwood V7 是我做的第二台 TPU,V8 是我做的最后一代 TPU。在我离职之前，很开心今天能跟大家分享一下我对 TPU 的一些理解吧。首先我想说 TPU 是一个上下游产业链非常复杂的产品。所以它是里面牵扯了很多软件硬件包括代工。所以里面很多很多技术细节是非常复杂的。也是一个非常庞大的一个工业体系。所以我希望我今天只是根据我的理解，去帮助大家更加解惑一下 TPU 这一层神秘的面纱吧。

**泓君:**
[(02:19)](https://podwise.ai/dashboard/episodes/7501932?locate=139)
你刚刚提到了你其实参与了 V7 跟 V8 的开发。然后据我了解，这个其实是谷歌的 TPU, 他们开始去适应大模型的这种域性链。最核心的，然后也是最有竞争力的这两代的芯片，也是最新的两代。因为你刚刚提到了，它可能涉及到软件硬件，然后包括代工生产。所以今天我觉得我们可以从硬件、软件生产环节，每个环节都把它就是详细的剖析一遍。包括现在谷歌的 TPU 跟像 Anthropic, 像 Meta 这之间的合作。我们可以看看它是如何影响到整个未来的英伟达的生态的。最开始就是第一个问题。很多人他并不清楚，我们说 TPU 跟英伟达现在的，比如说 GB 200 或者 H 100 的 GPU, 它有什么样的不一样。我们可以先简单给听众介绍一下 TPU 跟 GPU 它的不同之处，尤其是它在预训练方向的不同之处。

**Henry Zhu:**
[(03:15)](https://podwise.ai/dashboard/episodes/7501932?locate=195)
TPU 和 GPU 本身它的架构是完全不一样的。然后 GPU 大家可能更加了解更加多一些，因为它是最开始做游戏的显卡，所以它里面会用到一个数叫 SimT，它是一个 SimT 的架构，Single Instruction Multiple Threading，它是个多线程单一指令的一个架构，所以可以理解成一个厨房里面同时安排着很多很多个大厨，他们每个人都有自己独立的思考能力，然后独立去从冰箱里把东西拿出来，然后做完菜，然后最后上菜，他们可以独立完成这样一件事情，所以你安排很多很多大厨的话，这样的话，并行计算能力就会非常非常的强。

**泓君:**
[(03:54)](https://podwise.ai/dashboard/episodes/7501932?locate=234)
这个是 GPU，那如果我们同样用做菜来比喻 TPU 的话，你觉得它的流程跟 GPU 有什么不一样？

**Henry Zhu:**
[(04:00)](https://podwise.ai/dashboard/episodes/7501932?locate=240)
TPU 和 GPU 最大的区别就是 TPU 是一个针对机器学习的一个加速器。我们知道机器学习任何的算法，包括最开始的 CNN 到现在的 Attention 到到 Transformer, 到未来的架构，里面的核心就是矩阵计算。我们知道矩阵计算这个东西是非常 computer bound，是一个 computer intensive 的一个工作。TPU 的话就是针对这个矩阵计算专门做了一个定制的加速器。所以你可以理解成做饭的话，我们是一个流水线，我们知道今天要做哪道菜，然后 TPU 的话相当于是把不用安排那么多的大厨，但是他会把大厨安排在每一个步骤上，会把每一个步骤都告诉你具体做哪个步骤的火，然后你做完之后直接把你做完的，比如说第一个人会从冰箱里把菜给取出来，第二个人直接把第一个人做完的部分的中间菜的结果做加工传到第三个人。然后相当于中间每一步骤你可以理解成是心脏的泵血，当你每泵一次它就会把血液传输到你身体的各个角落，所以这样的话中间它会少很多的调度，少很多的调控，所以这样的话它能保证每一个计算单元的使用率会更高一些。

**泓君:**
[(05:16)](https://podwise.ai/dashboard/episodes/7501932?locate=316)
所以我理解其实就是 GPU 它是这种有点类似于并行的，就是你每一个环节它是独立运行的，然后 TPU 是一场接力赛。在模型的训练上，这两种不同的架构。它的各自的优势跟缺点是什么？

**Henry Zhu:**
[(05:36)](https://podwise.ai/dashboard/episodes/7501932?locate=336)
我觉得现在域训练包括后面的 inference, 我们现在慢慢地从一个计算 computer bound 变成 memory bound, 所以我们现在对内存的要求是非常非常高的，所以 CMD 它架构它有一个缺陷，就是因为你需要独立地去做这样的计算的任务。所以你有时候会一直在等，数据搬运过来的过程当中，你就会有一些闲置的 idle period。所以这个过程当中的话，就会导致它的那个矩阵计算的利用率没有那么的高。它的 utilization rate 就会有下降。然后 TPU 就是根据这个特点，然后弥补这样一个缺点吧，我这么觉得。然后它相当于是，我们当然可以具体聊一下它的软硬件的一个协同的一个效应，它会保证它是一个满公寓下的一个运行，然后它不需要等待数据搬迁的过程过程它要么利用当地的缓存把一些提前搬运过来的数据重复去利用，或者说把我们的带宽跑满，这样我们可以把那个 memory utilization rate 也能达到一个峰值。

**泓君:**
[(06:41)](https://podwise.ai/dashboard/episodes/7501932?locate=401)
所以如果整体上我们用一句话来去做结论的话，你觉得 TPU 跟 GPU 他们在这个模型的训练上就是谁更强，然后能优化多少？

**Henry Zhu:**
[(06:53)](https://podwise.ai/dashboard/episodes/7501932?locate=413)
我觉得从域训练上来讲的话，目前 GPU 和 TPU 最大的区别就是当 GPU 现在也开始往那个方向去发展，因为 TPU 一直做一个 system level，它是一个大的计算集群，而不是单卡单芯片去做域训练，GPU 可能很长一段时间都是单张卡的性能非常非常的好，但是它没有一个网络，TPU 一直是主打一个 TPU pod, 它是一个有几千张卡的一个协同的一个训练的一个状态，它里面牵扯到了很多通信。我们当然可以继续聊一下 ICI, Interchip Connect。它芯片与芯片的通信和这个网络，我们叫做 3D Torus, 一个拓扑的网络，它可以让几千张卡芯片，它用户的感觉当中是一张卡的芯片，它中间的训练效率是非常非常高的，这样的话它的成本也是可以打得下来。

**泓君:**
[(07:44)](https://podwise.ai/dashboard/episodes/7501932?locate=464)
因为我是看新闻报道说谷歌 V7 的，就是你研发的这套 Ironwood 的芯片，它在物理参数上已经非常接近这个 GB200 了，所以他如果在真实的这个工作中，或者我们这样说，就是我们同样是训练一个 Gemini 的模型，同样一代的模型，同样的这个参数量，那用 GPU 跟用谷歌的 TPU 谁更省钱？

**Henry Zhu:**
[(08:11)](https://podwise.ai/dashboard/episodes/7501932?locate=491)
对，我觉得这是很好的问题。我的理解是针对自家定制的大模型，谷歌的话就是 Gemini, 将来如果说谷歌给其他大公司 Frontier 大模型公司定制的话，我觉得谷歌性价比是更高的，性价比我指的就是 At TCL, the total cost of ownership. 就是它的成本会更加有优势。当你知道你的 workload 是什么的时候，你就可以根据你的 workload 去做一些不管是物理，芯片层面的一些定制，或者说在软件层面去定制。虽然说它可能有点黑盒的感觉，但是就是我知道相当于给出了你所有的 assumption, 你所有的已知条件都确定下来，那我觉得 TPU 在现实生活中，在现实条件下，它的训练效率还是 TCU 都是会比 GPU 更加强大的。刚才我也提到这两点，它的 utilization rate 是更好的原因，第一是它的一个 flops, 它的 flops 我们指的就是单位时间内它做多少次浮点式运算，对，所以这样的话，因为 TPU 它里面主要的架构就是矩阵计算，所以它软件和硬件可以保证它每次，每个时间，每个计算单元它都有活在做。我们的软件相当于是帮助硬件说，哦，我不会让你闲下来，每个时间点你都给我做活。但是你具体做什么活是我告诉你的。你不需要去精准地去预测或怎么怎么样。所以在硬件层面说我们不会加很多的控制单元。这跟 GPU 很大的区别就是我们不需要任何的 prediction。它 prediction 那一层 level 相当于都是在软件层面去实现的。对，相当于你把硬件变得更蠢了一点，相当于是一个机械式的劳作，软件那边帮你把所有 complexity 都给处理掉。

[(09:56)](https://podwise.ai/dashboard/episodes/7501932?locate=596)
所以这样的话，Ironwood 主要有两大的进步，一个进步就是他把他的 flops, peak flops，数值上跟 GPU 更加接近了。然后另外一个点我觉得非常非常重要，就是它的 memory bandwidth 也是有一个巨大的提升。首先它肯定是用了更大的 HBM 保证了一定的带宽。然后第二点就是这个 HBM 的带宽它能被软件充分的去利用起来。

**泓君:**
[(10:20)](https://podwise.ai/dashboard/episodes/7501932?locate=620)
更高性能的 HBM 它现在在市场上从供应链的环节它好找货吗？

**Henry Zhu:**
[(10:25)](https://podwise.ai/dashboard/episodes/7501932?locate=625)
非常难找，因为 HBM 的话首先它是一个非常非常有点垄断的一个感觉，总共就三家公司垄断这个生产，应该是 SK Hynix，三星和 Micron。英伟达一直是 HBM 最大客户，然后 TPU 的话一直是相当于是一个 secondary 的一个 customer。所以你要跟那三家公司去确定一个订单的话，我觉得是需要一个良性的合作关系。之前的话，TPU 一直没有办法获得那么好的 HBM，或者说那么大的订单。

**泓君:**
[(11:00)](https://podwise.ai/dashboard/episodes/7501932?locate=660)
那现在谷歌 TPU 它的产量是多少？

**Henry Zhu:**
[(11:02)](https://podwise.ai/dashboard/episodes/7501932?locate=662)
具体数据我不是非常清楚，但我觉得也是在一个慢慢的爬坡的过程当中，谷歌 TPU V7 之前的话一直是有一个产能的问题，我觉得也是一个很多因素导致的，因为毕竟 V7 之前我们一直没有一个对外的一个生态，所以我们更多是针对内部的一个 deploy 和使用，所以我们没有办法和 Broadcom, TSMC 或者刚才所说的那几家 HBM 的厂商去锁定一个很大的一个订单。因为产能都是提前一年或两年去锁定的。当你没有那么大的客户或没有那么大的需求的话，就是你临时想去调整是比较困难的一件事情。然后第二点我觉得就是一个 CoreOS，就是 CoreOS 是 TSMC 的一个 capacity, 我们可以理解成我们现在的芯片都是跟以前完全不一样，因为我们现在都是做一个 code design, 我们的 HBM 内存芯片和计算芯片是两块独立的芯片，通过一个 2.5D Stacking 的一个封装，把它封装成一个集成芯片。这个的话，TPU，谷歌自己做不了，Broadcom 也做不了，它只能依赖于 TSMC。所以既然 Samsung 给你分配多少的产能，你就能用你达成多少的产能。

**泓君:**
[(12:14)](https://podwise.ai/dashboard/episodes/7501932?locate=734)
所以 TSMC 它去分配产能的时候，它也可能会看这个谷歌它要的量是多少。如果你的量大的话，比如说像英伟达，它的量就比较大，它是不是可能会先去做这个英伟达的量，凭这个订单量说话，然后你这个 Cohost 是指在它的整个的封装环节的一个部分，就是我们可以把现在产能的瓶颈，如果我们要去整体的总结一下，就是你刚刚提到的 Cohost 跟 TSMC 这块是在封装上的一个产能，然后还有一块就是 HBM，它是高带宽的内存供应，然后这个可能也是一个瓶颈，就是有三大垄断巨头去把持，所以产能主要是这两块，卡在这两块上。

**Henry Zhu:**
[(12:56)](https://podwise.ai/dashboard/episodes/7501932?locate=776)
对，还有一点就是两律。两律的话就意味着当你 TSMC 把一个 Wafer 生产出来之后，它上面这一块 Wafer 上有多少是合格的芯片。因为 TPU 和 TPU 它的制造理念是不太一样的。TPU 它更多是主打一个通信，它是主打一个芯片与芯片之间的通信。然后这样的话导致它的中间的失败率会比 TPU 更高一些。然后一旦失败了，因为它保证整个系统里每张芯片都是性能大致是一样的，如果有层次不齐的话，它整个系统跑下来的效率就不会有那么的高，但 GPU 可能就不存在太大这样的问题，因为我们都知道 GPU 有自己阉割的版本，就是 H100A100，所以一旦良率不好的话，它可以往下降级，但是 TPU 不可以，但你们也有之前，比如说你现在有 V8, 然后你有 V7，你还有 V6，它不可以降级吗？很难，因为是定制的芯片，价格都会不太一样，对，所以比较难，不是通用的芯片，所以一旦你良率不行的话，就这一块芯片相当于报废了，所以相当于你产能就没办法一下子提上来。

**泓君:**
[(14:06)](https://podwise.ai/dashboard/episodes/7501932?locate=846)
所以这个是我们刚刚说到的就是生产环节中的，然后生产环节取决于你的订单的量，因为 Anthropic 它其实是有跟谷歌说我要采购你 100 万的 TPU, 一百万应该也是一个很大的订单。因为达现在它的比如说 GB200 或者 H100 这些可能也是一个百万级到千万级。所以其实我觉得谷歌现在看起来这个 TPU 已经是在蚕食这一块的市场份额。

**Henry Zhu:**
[(14:38)](https://podwise.ai/dashboard/episodes/7501932?locate=878)
对我觉得我觉得 TP 和 GPU 就很难是同一个维度的去考量吧或者评价。我觉得 Anthropic 这个订单确实是挺大的一个订单。我觉得第一有很多个因素吧。第一，我觉得 Anthropic 和谷歌是一个相对于内循环，因为 Anthropic 很多投资方也是谷歌的。所以我觉得 Anthropic 和谷歌是一个深度合作的一个关系。相当于如果说是 Meta 或者其他的公司的话，我不确定它的成本，它的 TCO 到底能不能压得下来。然后第二点的话，我觉得就是 Anthropic 它的工程师的技术能力还是非常非常强的。所以他们能去用 TPU 来部署他们自己的自家的模型。然后就我们当然可以详细聊一下，为什么 TPU 的部署在一般 external third party customer 上那么的难。但我觉得 Anthropic 目前来讲是有这样一个谷歌的生态的，我是这么觉得。所以 Anthropic 拿下这个订单，我觉得也是有很多因素在里面。

**泓君:**
[(15:37)](https://podwise.ai/dashboard/episodes/7501932?locate=937)
那我们就顺着继续讲一下 Anthropic。它是不是跟谷歌的整个的软件生态有关系？因为我们说在 TPU 上要搭一层软件，然后我听说过这样一个说法，就是说因为 Anthropic 他们的很多人最开始是在 Google 的，所以他们其实是非常了解 TPU 上的这一套软件生态的部署的，所以他们可以用 TPU 去训练模型。

**Henry Zhu:**
[(16:01)](https://podwise.ai/dashboard/episodes/7501932?locate=961)
回答这个问题之前，我们可以先简单聊一下 XLA 吧。因为我觉得 XLA 也是谷歌的一个，你可以理解成黑盒，也可以理解成是一个 secret sauce。对，我觉得 XLA 和 CUDA 最大的区别，XLA 是一个静态的编译器，然后静态编译器指的就是当你的 workload 是已知的或者给定的话，它可以在一个全局的 TPU pod 的一个 system level, 它可以把它做一个全局的优化。优化分很多层面，我知道就是他们会做很多内部的算子融合，比如说你一个 kernel 里面有很多计算，然后你还会把一些计算合并到一个计算单元，这样的话，你能更好地去利用你的 Systemic Array 里面的矩阵计算的利用率，然后你中间的中心态结果你不用反复地存进内存，存出来。XLA 会帮你自动做这样的优化，然后它还会做很多的一些内存的管理，怎么去更好地去分布你的内存。它知道 TPU 喜欢怎么从内存里面去把数给读出来。这些东西都是跟硬件架构和一些具体的细节是非常非常相辅相成的。所以 XLA 会通过一个黑盒帮你去优化这样的一件事情，但问题就是这个事情你是没办法去很好去 debug 和去控制。

**泓君:**
[(17:19)](https://podwise.ai/dashboard/episodes/7501932?locate=1039)
对，就开发者他很难去用 XLA 这套系统，然后一个是编程语言他们不会对吧。它用的是什么编程语言？

**Henry Zhu:**
[(17:27)](https://podwise.ai/dashboard/episodes/7501932?locate=1047)
XLA 它是一个编译器，它上层是 PyTorch,JAX 和 TensorFlow。相当于你的开发者你可以写任何一样的语言，然后它通过 XLA 可以把你转化成 TPU 的 assembly code, 就是 TPU 的指令，它中间是做一个翻译的工作，翻译加优化。

**泓君:**
[(17:47)](https://podwise.ai/dashboard/episodes/7501932?locate=1067)
所以如果对应英伟达的话，对应的就是 CUDA 的整个生态体系。

**Henry Zhu:**
[(17:53)](https://podwise.ai/dashboard/episodes/7501932?locate=1073)
对，对。

**泓君:**
[(17:53)](https://podwise.ai/dashboard/episodes/7501932?locate=1073)
那我们说就是我们刚刚提到，其实 Anthropic 他们是有工程师去懂 XLA 的这套体系的。然后 Anthropic 它现在应该已经是用谷歌的 TPA 去出现了一些模型，对不对？

**Henry Zhu:**
[(18:05)](https://podwise.ai/dashboard/episodes/7501932?locate=1085)
对，对。

**泓君:**
[(18:05)](https://podwise.ai/dashboard/episodes/7501932?locate=1085)
就已经有这样的一些成果了。因为我们知道在 GPU 去训练模型的过程中，工程师经常会遇到的问题就是说它有一个 bug, 然后我需要去解决它，然后我需要在它的这个软件的编译器上，比如说 CUDA 的生态上去看看是哪个环节出了问题，然后把它解决掉。那现在如果是用谷歌的 TPU 出了问题，刚刚你也提到了 XLA 它是一个黑盒，那要怎么去解决呢？是必须找谷歌的工程师来解决，还是他们自己就可以解决？

**Henry Zhu:**
[(18:36)](https://podwise.ai/dashboard/episodes/7501932?locate=1116)
你可以理解成 XLA 是黑客，但是它里面有很多很多的帮助你去 debug 的一些工具，而且 function。但它问题就是你需要对硬件是有一些了解，你才能更好地去分析它。因为它 XLA 它会把很多算法做融合，然后内存做管理，这样的话它是一个 graph，它不是一个单个的计算，它是一个链条，一个图表，所以相当于你这样 debug 起来，它对工程师的要求会更，它是可以 debug，但对工程师的要求会更高一些。

**泓君:**
[(19:05)](https://podwise.ai/dashboard/episodes/7501932?locate=1145)
会更高，所以 Anthropic 找你们了吗？在训练的过程中。

**Henry Zhu:**
[(19:09)](https://podwise.ai/dashboard/episodes/7501932?locate=1149)
因为一旦发现 XLA 的 bug，你就必须让 Google 的工程师来去帮忙去分析去解决。如果真的是个 bug 需要去解决，就外部的开发者很难独立去处理一个或 fix 一个 bug, 不像 Kuda 的话是有一个很好的生态你可以去，对。

**泓君:**
[(19:24)](https://podwise.ai/dashboard/episodes/7501932?locate=1164)
所以你们大概比如说因为有卖一些 TPU 给 Anthropic, 你们会有多少人的一个工程师团队去支持他们，

**Henry Zhu:**
[(19:34)](https://podwise.ai/dashboard/episodes/7501932?locate=1174)
我是做硬件的嘛，我们有很多软件组专门去对接不同的客户，像我们现在可能之前他们是有 Apple，有 Metagene，然后 Anthropic, 他们都是会有很多很多的 request, 就是我离职之前的话也听说他们都是非常非常忙的。

**泓君:**
[(19:52)](https://podwise.ai/dashboard/episodes/7501932?locate=1192)
是的，是的，是的，我昨天听说就是用谷歌的 TPU 最大的团队其实不是 Anthropic, 虽然他们下了一个 100 万的订单，之前模型上也有用过，最大的团队其实是苹果。对，然后苹果是因为就是庞若明之前在这个 Google，然后他去苹果的时候，他就是相当于把谷歌的一整套带过去了，然后又直接用的这个 TPU 去训练他们的这个大模型。

**Henry Zhu:**
[(20:17)](https://podwise.ai/dashboard/episodes/7501932?locate=1217)
就我觉得也牵扯到一个软件债的迁移嘛，就是你要用 TP 的话，你必须要把原来的一套软件债全部迁移到 JAXXLA 上。如果你不牵引的话，你当然可以去做，它能跑，但你就利用不到 TPU 那么好的性能和 TCU。

**泓君:**
[(20:34)](https://podwise.ai/dashboard/episodes/7501932?locate=1234)
就要把它的性能榨改。

**Henry Zhu:**
[(20:36)](https://podwise.ai/dashboard/episodes/7501932?locate=1236)
因为如果现在目前 TPU 它最，相比 GPU 它最大的优势就是它成本控制得很好，相比起芯片它更多是依赖于软件。

**泓君:**
[(20:45)](https://podwise.ai/dashboard/episodes/7501932?locate=1245)
那你觉得 Google 的这个 Gemini 它的模型，因为它现在是应该是市场上最好的模型，他的训练出来跟 TPU 的关系有多大？

**Henry Zhu:**
[(20:56)](https://podwise.ai/dashboard/episodes/7501932?locate=1256)
我觉得关系是很大的。

**泓君:**
[(20:57)](https://podwise.ai/dashboard/episodes/7501932?locate=1257)
能占决定性因素吗？还是算法其实是占决定性因素的一部分？

**Henry Zhu:**
[(21:01)](https://podwise.ai/dashboard/episodes/7501932?locate=1261)
算法当然很重要，因为我觉得现在就是训练，特别是 pre-training, 它 capex 很大，然后它算法我觉得 D-Mine 里面有非常非常前沿的算法。TPU 能帮你去做的一件事情，当你有一个很好的算法之后，然后你提前跟 TPU 子沟通好，我是怎么样的一个算法，我是怎么样的一个 workload，我给你去定制这样的一个加速器，这样的话我能帮助你一个更短的时间，更快的迭代，去测试这样一套算法。我觉得这是一个很重要的事情，就是如果说你的训练效率很差的话，你需要花更长的周期去验证一套算法是否能 work, 但 TPU 如果能把你的时间打下来之后的话，迭代速度就会更快一些。

**泓君:**
[(21:47)](https://podwise.ai/dashboard/episodes/7501932?locate=1307)
对，你们定制这样的一个芯片大概要多久流程？

**Henry Zhu:**
[(21:51)](https://podwise.ai/dashboard/episodes/7501932?locate=1311)
对，我觉得这是任何芯片公司的一个痛点，包括 TPU, 包括 GPU，他们很多时候都是在预测未来市场的变化，包括预测未来模型的变化，之前知道 MOE 的话也是很早有这样一个想法，MOE 很长一段时间在 TPU 和 GPU 上跑的效果都不是特别好，但是后来 TPU 和软件一起增加了针对 MOE 的优化。

**泓君:**
[(22:17)](https://podwise.ai/dashboard/episodes/7501932?locate=1337)
那是哪一年？

**Henry Zhu:**
[(22:18)](https://podwise.ai/dashboard/episodes/7501932?locate=1338)
应该是 V4, V5。哦，V4, V5。那好早啊。

**泓君:**
[(22:23)](https://podwise.ai/dashboard/episodes/7501932?locate=1343)
V4, V5 应该是在模型初端。刚刚开始出来的时候。

**Henry Zhu:**
[(22:29)](https://podwise.ai/dashboard/episodes/7501932?locate=1349)
对对对。具体是 2022 年 23 年 23 年 23 年对对对。

**泓君:**
[(22:34)](https://podwise.ai/dashboard/episodes/7501932?locate=1354)
那太早了。

**Henry Zhu:**
[(22:35)](https://podwise.ai/dashboard/episodes/7501932?locate=1355)
可以详细介绍一下，就是为什么之前 MOE 一直在 TPU 上跑不起来的原因就是因为它是一个 2D, Tower, 相当于你每个 TPU 芯片只能跟它的 neighbor 相应 TPU 芯片做通信。它不是一个 all-to-all 的一个 communication。这意味着如果说 MOE 的话，它要有一个 routing 的一个 face, 它需要把它的 routing 给不同的专家和不同的专家分布在不同的芯片上。所以如果说你没有一个 all-to-all 的 connection 的话，这样的话，中间就相当于你要找一个朋友，你中间要经过很多个环节。经过很多这样的话，有很多的浪费，很多的拥堵，然后后来在 V4 的时候，TPU 推出了一个叫做 3D Taurus，它用了一个叫做 OCS 一个光铅的一个交换机，它相当于就是一个 software programmable 的一个交换机，相当于的话，你带一个 TPU pod 的一个节点，你可以通过软件来更改你想通信的那个 TPU 集群的一个路径，这样的话就相当于一下子把 MOE 的一个痛点给去解决了，然后之后的话 MOE 就在 TPU 上跑起来效率就会明显比之前高很多。

**泓君:**
[(23:45)](https://podwise.ai/dashboard/episodes/7501932?locate=1425)
所以我理解这个迭代是以年来计算的，因为你设计出来了还要量产对吧？

**Henry Zhu:**
[(23:49)](https://podwise.ai/dashboard/episodes/7501932?locate=1429)
对对对。

**泓君:**
[(23:50)](https://podwise.ai/dashboard/episodes/7501932?locate=1430)
量产的节奏一般是多久啊？半年半年打得下来吗？

**Henry Zhu:**
[(23:54)](https://podwise.ai/dashboard/episodes/7501932?locate=1434)
打不下来。对，我觉得看就是每家公司的 philosophy 都不一样，像 TPU 的话，一直都是主打说我一定要做一个旗舰的一个 training chip 或旗舰的 influence chip. 一直以来都是这样的一个想法，所以说这对快速迭代要求会非常非常高，因为你每一代芯片都会塞很多很多样的 request 嘛，然后相当于整个流程，当你设计完，然后我们会做验证，然后再交给 Broadcom 去做通信上面的那些设计，然后在整个 package level 再做验证，最后交给 TSMC。我觉得整个流程最快最快也要两年到两年半，三年。

**泓君:**
[(24:34)](https://podwise.ai/dashboard/episodes/7501932?locate=1474)
所以我们今天看到的就是你刚刚提到谷歌已经更新到 V7, V8 的这一款了，这一款我理解它在设计上它是两年以前就做了，但是我们说就是模型的更新，包括模型里面大家核心的这个算法往哪个方向走，最近我觉得基本上是每 6 个月可能就会变化一次。也就是说你要在两年前去预测两年以后的模型它在往哪个方向走。

**Henry Zhu:**
[(25:02)](https://podwise.ai/dashboard/episodes/7501932?locate=1502)
对。

**泓君:**
[(25:02)](https://podwise.ai/dashboard/episodes/7501932?locate=1502)
压对了吗？

**Henry Zhu:**
[(25:03)](https://podwise.ai/dashboard/episodes/7501932?locate=1503)
目前来讲 V7 应该是压对了。这也是为什么可以解释一下，为什么之前 TPU 芯片和 GPU 在纸面参数上是有一些挺大的距离的。因为之前 TPU 的主要的 workload 是内部的一些 recommendation 和 ranking system 的算法。所以他可能没有想到 GPT 出来之后，有这样一个大的需求和对算力上一个那么大的一个需求。他针对 recommendation ranking 是完全不一样的 workflow 的。所以他里面我之前我参与也是设计了一道一个模块，叫做 SparseCore 西数计算的一个模型。它是专门针对一个 recommendation 的一个 embedding, 它这个 layer 的一个算法做了一个特定的优化，但那个优化里面它没有很稠密的矩阵计算，它更多是比较稀疏的矩阵计算，所以我们会针对它，我们做了很多算法，所以它牺牲掉了一些那个矩阵的那个算术的密度。那时候我们发布出来之后，感觉 GPU 好像做矩阵计算好像比 TPU 更好了一点。

**泓君:**
[(26:01)](https://podwise.ai/dashboard/episodes/7501932?locate=1561)
是微集的版本。那时候应该是 V4,V5。他开始把他所有的这个核心性能往大模型的预训链上调，是哪一代，然后是哪一年？

**Henry Zhu:**
[(26:13)](https://podwise.ai/dashboard/episodes/7501932?locate=1573)
对，应该就是 V6 吧。

**泓君:**
[(26:15)](https://podwise.ai/dashboard/episodes/7501932?locate=1575)
V6 开始的。

**Henry Zhu:**
[(26:16)](https://podwise.ai/dashboard/episodes/7501932?locate=1576)
V6 开始我记得应该是做了两个版本，一个是专门做训练，一个专门做 inference。对，训练和 influence 其实本质上它们是用的是同一套架构，influence 可以理解成是训练芯片的一套字体吧，它可以有更小的运算单元，它可以有更小的内存，因为相比 training 的话，influence 它不需要做 backpropagation, 反向传播，所以它不需要很多的那种中间，不需要存储很多的副本。

**泓君:**
[(26:46)](https://podwise.ai/dashboard/episodes/7501932?locate=1606)
差不多是在，也是在两年多，两三年以前去开始调整这个方向的。其实 GPT 它是在 2020 年它就有一个版本放出来，当时是 GPT3 嘛，就谷歌那个时候它已经开始就是意识到这是一个 next big thing, 然后它有去做这个芯片上的调整或者 TPU 上的调整。

**Henry Zhu:**
[(27:06)](https://podwise.ai/dashboard/episodes/7501932?locate=1626)
对，其实谷歌很早就开始，因为 Transformer 也是 Google 出来的嘛，谷歌很早就开始知道 Transformer 这套架构，然后根据这套架构，我们芯片组我们也知道，我们会是做这一套架构的一些去优化。包括 Tension Kernel，很早就已经在内部里面就知道了。

**泓君:**
[(27:24)](https://podwise.ai/dashboard/episodes/7501932?locate=1644)
你这样说我突然想起来，就是我觉得谷歌的 TPU 到现在，它能在整个的模型域训练中性能表现非常的优秀。也是因为 Transformer 这套架构是谷歌发明的，然后从 TPU 的这个诞生开始，它就一直走的是这一条的路径，所以我们其实也可以把 TPU 理解成一个针对于大模型，预训练或者推理的 ASIC，这样说是对的吧？

**Henry Zhu:**
[(27:49)](https://podwise.ai/dashboard/episodes/7501932?locate=1669)
没问题，没问题。

**泓君:**
[(27:50)](https://podwise.ai/dashboard/episodes/7501932?locate=1670)
对，然后你这个 ASIC 就一定要压对方向，它压到了 Transformer。

**Henry Zhu:**
[(27:54)](https://podwise.ai/dashboard/episodes/7501932?locate=1674)
没问题，对。因为我觉得有点 insider news 的感觉。我们知道 transformer model 大概模型是什么架构，比起行业外我们更早知道这样的一手信息，更早知道它是怎么样的一个 workload, 所以我们相当于是有一个先发的优势，但我们的问题就是本质上 ASIC 和 GPU 它就是一个是通用性好，一个是通用性不好，就是说即使我们没有一手的信息，但是你一旦确定了 ASIC 它是没办法去改变的，所以说 GPU 甚久甚短，它因为现在模型迭代周期非常非常短，都是以月来做单位，所以一旦你的芯片架构固定下来之后，你一旦有一个新的算法上的一个动态的一个改变的话，你在 TPU 上就很难去实现这样一些事情。即使你有先发优势，但后面的竞争会越来越激烈，这样的话你的先发优势可能也会慢慢被蚕食。我觉得有一个阶段就是 GPU，因为它通用性好，所以它 GPU 上去跑模型的迭代速度会比 TPU 更快一些。

**泓君:**
[(28:53)](https://podwise.ai/dashboard/episodes/7501932?locate=1733)
那现在是哪个阶段，TPU 更快？

**Henry Zhu:**
[(28:55)](https://podwise.ai/dashboard/episodes/7501932?locate=1735)
目前 V7 是感觉跟 GPU BlackHawk 已经是旗鼓相当了。

**泓君:**
[(29:01)](https://podwise.ai/dashboard/episodes/7501932?locate=1741)
旗鼓相当，那 8 呢？

**Henry Zhu:**
[(29:02)](https://podwise.ai/dashboard/episodes/7501932?locate=1742)
VIA 的目前不能透露，但是技术细节肯定是不能透露，但是我刚刚提到 MOE 这样一个架构，TPU 在芯片或通信层，它就解决这样一个问题，但是我担忧的一点就是这一套架构它的 scaleability 到底有多强，如果说将来有一个完全不同的一个方式，完全不同的一个模型，你没办法在 TPU 上做这样的去优化。

**泓君:**
[(29:31)](https://podwise.ai/dashboard/episodes/7501932?locate=1771)
对。

**Henry Zhu:**
[(29:32)](https://podwise.ai/dashboard/episodes/7501932?locate=1772)
然后 GPU 上可以，这样的话你定制芯片因为 TPU 团队跟 GPU 是不是一个量级的，你没办法做一个快速的迭代，这样的话你的性能上差距可能就会被拉大。所以我觉得这个很难去评判现在，我觉得就是可能是我对 TPU 的一个担忧吧，就是万一将来有不同的范式，它怎么去。

**泓君:**
[(29:57)](https://podwise.ai/dashboard/episodes/7501932?locate=1797)
我们刚刚提到了就是如果整个大模型它的训练范式变了，然后就会影响到整个 TPU 它的销量。你的这个范式指的是，就是有多大的范式调整。我可以举个例子，就比如说如果我们整个以 Transformer 为底层架构的这样的一个范式编了的话，那我理解 TPU 没用了，对吧？我可以这样说吗？

**Henry Zhu:**
[(30:21)](https://podwise.ai/dashboard/episodes/7501932?locate=1821)
不完全准确，就 Transformer 它核心还是矩阵计算。

**泓君:**
[(30:25)](https://podwise.ai/dashboard/episodes/7501932?locate=1825)
对，矩阵计算的，如果这样的一个，但这样应该不会。

**Henry Zhu:**
[(30:29)](https://podwise.ai/dashboard/episodes/7501932?locate=1829)
应该不会。

**泓君:**
[(30:31)](https://podwise.ai/dashboard/episodes/7501932?locate=1831)
但是我们就是说如果出现了类似于不同于 Transformer 的新的架构，那 TPU 它就不一定有这个 GPU 它的表现好。那如果是在模型层面，就像你刚刚说的，比如说说有一些这种 MOE 的一些新的方法的微调，但它还是基于整个 Transformer 的，这种你觉得 TPU 未来它还是会持续的占优势吗？包括大家现在很热的说，我要用这个 RL 的方式去做 agent 的训练，或者去做模型的训练。就是 RL 对 TPU 的影响会大吗？

**Henry Zhu:**
[(31:09)](https://podwise.ai/dashboard/episodes/7501932?locate=1869)
肯定是会是不一样的 workload 的，肯定是会有影响，就是我觉得像 TPU 的一个策略就是把芯片变得更加的通用，我也是在往这样一个方向去发展，相当于它矩阵单元，它可以处理各种 workload 的矩阵计算，然后它也有专门负责去处理一些稀疏的矩阵计算的一些单元，它可以理解成把很多硬件上的一些计算和内存模块化，来适应将来不同的那个方式。所以从某种意义上来讲也是一个向通用性的一些妥协。

**泓君:**
[(31:41)](https://podwise.ai/dashboard/episodes/7501932?locate=1901)
了解。

**Henry Zhu:**
[(31:42)](https://podwise.ai/dashboard/episodes/7501932?locate=1902)
对，我觉得之前以往几代芯片可能设计的没有那么激进的原因，也是有一部分的考量。就是如果一旦你设计的非常激进，你可以在 Gemini 上模型效率可以提升 30% 到 40%, 但万一有变化呢？万一有变化，那你回去的话你就会非常非常冷。痛苦。所以我觉得这是一种基于现实的一种 compromise。

**泓君:**
[(32:04)](https://podwise.ai/dashboard/episodes/7501932?locate=1924)
然后我们刚刚提到了，就是整个在适应新的算法方向的从设计层面的调整。那还有一个我觉得谷歌最近也比较用力的就是在软件层。让整个的中间的软件能够适应更多的开发者的调整。我最近是看到一条新闻说这个 Google 跟 Meta 也有一个这样的 TPU 的采购协议，然后谷歌向 Meta 提供这种 TPU 的托管的服务。但是呢，就是他可能核心想做的就是把 PyTorch 变成这个 TPU 的一个中间层，类似于像谷歌的 CUDA 生态这样的一个，就是比如说大家在做编译器的时候可以用 PyTorch 来编译。

**Henry Zhu:**
[(32:45)](https://podwise.ai/dashboard/episodes/7501932?locate=1965)
我也有了解，当然具体的细节还没有没有公开嘛。所有的 Meta 都是用 Pytorch，大家应该是众所周知。所以 Pytorch，刚才我有提到它跟 TPU 的生态其实不是特别的兼容，所以它很难像 Anthropic 那样做一个深度的对 TPU 整个软硬件生态的使用。目前来讲，我觉得它可能更多是依托于谷歌云，谷歌 Cloud, 提供更多算力，因为包括它今年它其实股价不是那么好的原因，也是一个 KPAC 实在是太大了，它对特别预训链这一块的成本支出实在是需求非常非常的大。他已经把市面上所有能买来的算力都买来了，去做这样一件事情。所以我觉得 TPU 目前来讲也是去帮他去 offload 他这一方面的一些压力。软件态的话，Pytorch 很早之前就跟 TPU，包括 Fair 那个团队，跟 TPU 都有接洽，就说能不能在 TPU 上更好地去支持 Pytorch, 这样的话我们也更好地去做一些 research 的开发。但目前来讲，我觉得谷歌也是有在去做一些改变了。离职之前也知道有很多组在做 PyTouch 和 XLA 的结合，包括在 XLA 和 TPU 上支持很多 Native 的一些 PyTouch 的一些 Library, 一些并行的 Library 和一些算子。因为现在 PyTouch 的算子实在是太多了，它可能有好几千个算子，如果你不在硬件上原生的去支持这些算子的话，你的性能表现就会比较差一些。

**泓君:**
[(34:18)](https://podwise.ai/dashboard/episodes/7501932?locate=2058)
那所以如果大家直接用谷歌云呢？

**Henry Zhu:**
[(34:21)](https://podwise.ai/dashboard/episodes/7501932?locate=2061)
当然可以。

**泓君:**
[(34:22)](https://podwise.ai/dashboard/episodes/7501932?locate=2062)
问题是什么？

**Henry Zhu:**
[(34:23)](https://podwise.ai/dashboard/episodes/7501932?locate=2063)
问题就是直接用谷歌云的话相当于你把，你相当于是拖管了，你没办法做很多底层的去控制。如果你想去挖掘每一分的性能的话，你从谷歌云就没办法去做这样的事情。

**泓君:**
[(34:35)](https://podwise.ai/dashboard/episodes/7501932?locate=2075)
就是会有浪费。

**Henry Zhu:**
[(34:36)](https://podwise.ai/dashboard/episodes/7501932?locate=2076)
会有浪费。

**泓君:**
[(34:37)](https://podwise.ai/dashboard/episodes/7501932?locate=2077)
这个浪费大概会在一个多少的百分比？这个很难说，多少到多少，一个大概的比例，比如说 50%, 它可能是一个非常大的浪费，10%，它会稍微小一点。

**Henry Zhu:**
[(34:48)](https://podwise.ai/dashboard/episodes/7501932?locate=2088)
对，这就是我刚才说到的 model utilization rate, 如果说你结合的非常好的话，你能几乎满状态的达到一个 peak memory bandwidth, 但是如果用谷歌运营来跑的话，你很有可能用到只有 50%,60% 它的 utilization rate, 但是你还是要付同样的钱，对吧？

**泓君:**
[(35:06)](https://podwise.ai/dashboard/episodes/7501932?locate=2106)
那区别还是挺大的。所以现在 Meta 跟 Google 的合作，反而我是看新闻报道啊，我觉得好像是在这个谷歌的谷歌云上跑，对吧？

**Henry Zhu:**
[(35:18)](https://podwise.ai/dashboard/episodes/7501932?locate=2118)
对对对。目前我知道直接购买 TPU 计价的 Rack, 直接从 Blocker 购买应该只有 Anthropic, 其他目前都还是谷歌云。

**泓君:**
[(35:28)](https://podwise.ai/dashboard/episodes/7501932?locate=2128)
所以苹果跟 Mid Journey 也是在谷歌云上跑的？那这样也简单，就从谷歌招工程师过去，人才的流动，所以谷歌的工程师现在应该是就是还挺吃香的。

**Henry Zhu:**
[(35:42)](https://podwise.ai/dashboard/episodes/7501932?locate=2142)
对，我觉得如果将来有很多更多的第三方加入的话，特别是 JAX 在 XLA，这其实是一个非常非常高技术壁垒的一个工作吧，所以特别是我觉得如果之前在谷歌有这样的经验的话，能更好地迁移过去。

**泓君:**
[(35:59)](https://podwise.ai/dashboard/episodes/7501932?locate=2159)
多长时间能学会一个工程师？

**Henry Zhu:**
[(36:03)](https://podwise.ai/dashboard/episodes/7501932?locate=2163)
还是挺难的。

**泓君:**
[(36:04)](https://podwise.ai/dashboard/episodes/7501932?locate=2164)
挺难的。

**Henry Zhu:**
[(36:05)](https://podwise.ai/dashboard/episodes/7501932?locate=2165)
因为你需要了解硬件，也需要了解软件。

**泓君:**
[(36:08)](https://podwise.ai/dashboard/episodes/7501932?locate=2168)
它是一个复合技能。

**Henry Zhu:**
[(36:09)](https://podwise.ai/dashboard/episodes/7501932?locate=2169)
复合技能，对。

**泓君:**
[(36:10)](https://podwise.ai/dashboard/episodes/7501932?locate=2170)
然后我们看到就是在今年就是包括就是之前就很多开发者，他在调这个大模型的 API 接口的时候。其实我注意到谷歌的这个 API 接口的成本，大概最开始是只有 OpenAI 跟 Anthropic 的十分之一。同时呢这个 Anthropic 它最近的 4.5，他的这个 API 接口大概是下降了 67%, 然后我看有媒体报道把这部分的功劳归功于他是用谷歌的这个 TPU train 的，所以这两者是直接相关的。在推理成本上。

**Henry Zhu:**
[(36:46)](https://podwise.ai/dashboard/episodes/7501932?locate=2206)
推理成本上确实是这样。我觉得 Google 它现在的推理芯片成本确实会比 GPU 要高不少。它的原因就是刚刚所说它是一个集群的一个推理。它的 TCO 就能打得下来。刚才没有提到一点，就是 GPU 的集群，它用的是一种 NVLink 和 NVSwitch 这样的一种通信协议。这个其实很烧钱。它是一个你可以理解成是一种 Infrastructure 的一种 Text。所以你需要跟很多不同厂商去买这种交换机，然后部署在你的数据中心当中，这是一个很大的成本开支。谷歌因为它用了它不一样的一个拓扑架构，它用了一个是芯片与芯片之间直接通信，它用的是个桶，它不用交换机，然后它只有在某些节点上用一些光学交换机，它也比较少，然后实现了同样的一个通信的一个效果，所以它在一个成本支出上就是会比 GPU 要好很多。

**泓君:**
[(37:41)](https://podwise.ai/dashboard/episodes/7501932?locate=2261)
所以就是我理解在建数据中心的这一环成本已经拉开了。

**Henry Zhu:**
[(37:46)](https://podwise.ai/dashboard/episodes/7501932?locate=2266)
已经拉开了。

**泓君:**
[(37:47)](https://podwise.ai/dashboard/episodes/7501932?locate=2267)
NVIDIA 的主要成本是交换机。

**Henry Zhu:**
[(37:50)](https://podwise.ai/dashboard/episodes/7501932?locate=2270)
嗯。

**泓君:**
[(37:50)](https://podwise.ai/dashboard/episodes/7501932?locate=2270)
我了解到他们在搭的整个的这个芯片机群里面还要铺很多的铜，是不是他们都是用这种铜的这个线来连接起来的。

**Henry Zhu:**
[(37:59)](https://podwise.ai/dashboard/episodes/7501932?locate=2279)
对对。

**泓君:**
[(38:00)](https://podwise.ai/dashboard/episodes/7501932?locate=2280)
那谷歌的主要成本是什么呢？是夜冷吗？

**Henry Zhu:**
[(38:02)](https://podwise.ai/dashboard/episodes/7501932?locate=2282)
一脸是一块吧，其实跟英伟达也差不太多，它主要成本也是它的一些 Surges, 我们会跟 Broadcom 深度合作一些这种 Surges, 就相当于是把信号从一个芯片准确无误的传输到另外一个芯片，因为相比 GPU 的话，TPU 它更多依赖于 Surges 的一个稳定性，所以它的那块资本支出还是很高的。

**泓君:**
[(38:27)](https://podwise.ai/dashboard/episodes/7501932?locate=2307)
然后你们跟 Broadcom 的这个合作会持续多久？还是它是根据量来的，如果量大了谷歌可能就自己做了。

**Henry Zhu:**
[(38:34)](https://podwise.ai/dashboard/episodes/7501932?locate=2314)
对，因为现在市面上有两家公司做这样的代工，一家是 Broadcom，另外一家是 Marvel。然后 Broadcom 和 Marvel 最大的区别就是 Broadcom 有点像 2B，它是一个 2B 的 business, 它就锁定几个最大的客户，然后跟他们做深度的合作和定制。然后 Marvel 的话可能更多是跟一些中型或一些 Startup, 他们可能说提供一个 IP 的一个 Solution, 他可能不会根据你的模型去深度去定制一些硬件，但他可能会做一个料。然后 Broadcom 它好处就是可以帮它最大的客户去争取最大的产能，就是 Co-Op, TSMC。所以说一直以来 TPU 都是跟 Broadcom 去做这样一个合作，也是目前我不觉得会有很大的改变，但这样导致的一个不好的问题就是 Broadcom 的溢价权会越来越大，然后你在中间 TPU 能赚的 margin 也会越来越少。是的，如果你非常依赖 Blockchain，然后你没有一个 backup 的话，就是有点像 TPU 和 GPU 的关系，一直以来 TPU 是 GPU 的一个 backup, 也是有溢价权的考量在里面，所以如果你没有一个 backup 的话，你在成本上就很难控制下来，所以我觉得这也是将来可以关注的一个点。

**泓君:**
[(39:49)](https://podwise.ai/dashboard/episodes/7501932?locate=2389)
所以谷歌它能直接跟 CoreOS 这一块来溢价吗？决定的核心要素是什么？

**Henry Zhu:**
[(39:55)](https://podwise.ai/dashboard/episodes/7501932?locate=2395)
Cohost 主要还是那个 HBM，就是我们的那个内存的一个封装，所以这样的话相当于我的理解是 Broadcom 是一个中间的环节，它会把所有的东西全部帮你铺设好，然后它交给 TSMC 去量产，所以我觉得第一你需要去拿到一个很高的，很好的一个 HBM 的一个 capacity, 再通过 Broadcom 去跟 TSMC 争取一个好的 Cohost。

**泓君:**
[(40:19)](https://podwise.ai/dashboard/episodes/7501932?locate=2419)
我听说现在整个 HBM 的 capacity 被英伟达垄断了。

**Henry Zhu:**
[(40:23)](https://podwise.ai/dashboard/episodes/7501932?locate=2423)
基本上是垄断了，对。因为刚才有提到，因为 HBM 现在我们已经慢慢从 compute bomb 变成 memory bomb, 特别是像现在的 attention kernel，它其实核心就是一个你怎么样去更快地从内存里把数据给搬运出来，因为它的 kb caching 它里面有很多数据的一些搬运。未来几年的方向可能就是 HBM 能决定你的上限。如果你买不到好的 HBM 的话，那你的清理效率就会大打折扣。

**泓君:**
[(40:54)](https://podwise.ai/dashboard/episodes/7501932?locate=2454)
所以博通的主要核心的作用是在这一块。

**Henry Zhu:**
[(40:57)](https://podwise.ai/dashboard/episodes/7501932?locate=2457)
博通主要的核心作用它是做很多的通信，ICI, Interchip Connect，然后它会做很多的后端。因为我们芯片的话，我们 TPU 团队主要设计它的前端，比如说我们相当于是把一张图纸给打印出来，然后 BlockCon 会把每个芯片与芯片之间物理上去连接起来，然后最后交付给 TSMC 在物理上去生产出来，所以 BlockCon 更多是做一个信号的连接，怎么去布局这样一个突破的一个网络，所以 BlockCon 可能是在这一边。

**泓君:**
[(41:28)](https://podwise.ai/dashboard/episodes/7501932?locate=2488)
嗯，也是挺核心的一个环节。

**Henry Zhu:**
[(41:30)](https://podwise.ai/dashboard/episodes/7501932?locate=2490)
非常核心，而且是一个技术壁垒非常非常高的一个环节。因为现在目前一旦信号里面有问题的话，那整个集群就不能用了。所以这是一个你可以理解成脏活累活，但你也可以理解成这是一个非常非常吃经验的，也是技术壁垒非常非常高的。因为 Broadcom 他们那边的话主要是一个复合的一个信号，TPU 和 GPU 都是一个数字电路，但那个是一个 mixed signal, 是一个模拟电路加数字电路，所以它对经营要求会更高一些。

**泓君:**
[(42:04)](https://podwise.ai/dashboard/episodes/7501932?locate=2524)
就是如果我们用一句话来总结，你觉得现在谷歌的 TPU 可以挑战英伟达在 GPU 的垄断地位吗？或者说至少是我去阻挠一下英伟达在这个市场上绝对的定价权。

**Henry Zhu:**
[(42:19)](https://podwise.ai/dashboard/episodes/7501932?locate=2539)
没有绝对的答案，但我的结论是在某些限定的条件下，如果一旦满足了这些限定条件，我觉得 TPU 是完全可以挑战 GPU 的。限定条件就是一个大规模部署，因为 TPU 它主打的就是走量，如果说 TPU 它其实不太擅长做一件事情就是它没办法针对一个单用户，比如说做 agent，它就不太适合，因为它延迟会比较高一些。

**泓君:**
[(42:43)](https://podwise.ai/dashboard/episodes/7501932?locate=2563)
哦，推理芯片也不行是吧？

**Henry Zhu:**
[(42:45)](https://podwise.ai/dashboard/episodes/7501932?locate=2565)
现在的提升，但是它比起像 Grok，比起英伟达，它的可塑性就没有那么的强。所以它必须在一个非常大的一个吞吐量下，比如有很多很多用户同时去调用这个接口，很多很多用户同时去用 Gemini 和 ChatGPT, 它才能把这个成本给分摊开来，这样它能得到一个很好的一个吞吐量。所以在这种情况下，大规模部署，然后模型相对比较稳定，它不需要很多的变动，这样的话，它的整体的成本就相比 GPU 有很大的优势。那我的总结就是，当如果你的模型相对比较固定，不需要很多的改变，然后它的形态也是比较静态的形态，TPU 是非常适合去大规模部署的。那比如说你已经训练好了一套模型，然后你只需要去做 inference, TPU 它这个 system level 的优化能力，软硬件协作能力能帮助你的这一套模型，能把成本控制在一个非常好的范围之内，这也是 TPU 最大的优势，但它有它的前提就是它必须是一个非常大的用户在用，很多用户在用，它不太适合去做本地的部署，然后它很适合在云上去用，很适合比如说你 Gemini,ChatGPT,Claude 这种每天都有 millions of users, 这样的话它会把成本分摊给每个 user。

**泓君:**
[(44:06)](https://podwise.ai/dashboard/episodes/7501932?locate=2646)
但如果它去做推理的话，他的延迟会有多少，就是跟普通的这种 GPU 相关的这些 agent 来比。

**Henry Zhu:**
[(44:15)](https://podwise.ai/dashboard/episodes/7501932?locate=2655)
大家可以去看他官方的 documentation, 他的延迟做的也是非常非常不错的，但他没有写的就是他需要有一个大用户，他我们内部会有一个 batch 就是你可以理解成多少个 queries, 多少个 request，你如果每一秒你没有很多足够多的用户的话，它 TPU 没办法软件把它打包成一个很大的一个矩阵计算，然后就很好地去利用这个矩阵计算的这个计算密度，如果你只有你一个用户的话，那你的成本就会很高了。

**泓君:**
[(44:45)](https://podwise.ai/dashboard/episodes/7501932?locate=2685)
就是用户规模数越大，它反而越快。即使在推理上也是这样。如果你是一个非常小的这种创业公司，那大家可以用谷歌语音接入，然后我跟其他的 request 打包吗？

**Henry Zhu:**
[(44:59)](https://podwise.ai/dashboard/episodes/7501932?locate=2699)
因为 ThruPool 它其实有两个性能的指标，一个是吞吐量，就 ThruPool 市面上讨论比较多的 tokens per second，每秒产生多少个 tokens。还有另外一个就是我们的尾部延迟，就 critical pass 的 latency, 它可以保证一个非常快的响应速度，然后每一个 token 之间它的 latency 都是非常低延迟的，所以我觉得这一块的话，TPU 是不太适合去做这种非常快的响应速度。但它可以适合做就是它每秒产生 100token, 但是有些用户可能稍微慢一点点，然后有些用户快一点，保证一个平均值，这是它能做到的一件事情。

**泓君:**
[(45:36)](https://podwise.ai/dashboard/episodes/7501932?locate=2736)
谷歌的 Gemini 它是不是所有的模型全部是用 TPU 训的，还是它也会有加入 GPU?

**Henry Zhu:**
[(45:42)](https://podwise.ai/dashboard/episodes/7501932?locate=2742)
V7 的发布，基本上是说 100% 是用 TPU，但是在 V7 之前，TPU 一直是 TPU 的 backup, 原因也很简单，XLA 加 GX 是一套非常复杂也非常不稳定的一套软件生态，它很多时候会容易去报错，一旦报错，特别是在 pre-training, 它的风险是很大的，现在我觉得这套系统已经非常 steady 了，也非常成熟了，在之前不成熟的时候，你就需要用 TPU 来做 backup, 所以之前一直是 GPU 和 TPU 混着，混用训练，并不意味着 TPU 训练效果没有 GPU 好，只是意味着它的整个 system，它的稳定性还没有达到一个非常好的状态。

**泓君:**
[(46:25)](https://podwise.ai/dashboard/episodes/7501932?locate=2785)
我听说现在谷歌内部的 Gemini 就是非常缺 TPU, 因为我有时候用它模型的时候，它会说这个请求过多，然后就挂了，这应该是没有卡是吧？

**Henry Zhu:**
[(46:37)](https://podwise.ai/dashboard/episodes/7501932?locate=2797)
对，就是没有卡。

**泓君:**
[(46:38)](https://podwise.ai/dashboard/episodes/7501932?locate=2798)
这个情况会持续多久？

**Henry Zhu:**
[(46:41)](https://podwise.ai/dashboard/episodes/7501932?locate=2801)
具体不是非常清楚，但是现在可能就是大家一下子，因为我也看到了大家用户很多都从 ChatGPT 迁移到 Gemini 了嘛，对，要满足这样的很大用户的一个 migration 的话硬件上确实需要 share run time period。

**泓君:**
[(46:56)](https://podwise.ai/dashboard/episodes/7501932?locate=2816)
刚刚我们提到了其实它是有独立的训练芯片跟独立的推理芯片的。哪个现在的量更大？是这样，就是我其实就是如果不说具体数据也没有关系，我其实是想给听众一个大概的印象，就是想让大家看一下这个训练跟推理，他们各自的大概的一个比例跟综合情况是怎么样的。

**Henry Zhu:**
[(47:17)](https://podwise.ai/dashboard/episodes/7501932?locate=2837)
Ironwood 其实它是一款主要针对 Infrared 推理芯片的一个性能上的一个优化的一个芯片。它当然可以做训练，但它里面很多的一些核心的黑科技，是针对我们现在当下的一个推理的一个应用的市场。所以它比起训练它要求的那些指标，Io 的更多是它要保证你一个低延迟，保证你一个大的存储量，保证你的 memory bandwidth 是足够大的，这样的话，你做 LLM 特别在 decode 那个环节，你不会被内存那边卡住。所以说，我觉得这也是一种信号，就是说谷歌觉得未来，包括整个市场，我觉得未来对 influence 这个成长的潜力还是非常非常看好的。但 pre-training 也是非常重要的一个环节，因为谷歌作为少数几家做 frontier 大模型的公司，他们一直以来，他们的 Philosophy 就是我设计一款芯片，首先是要把 training 做好。

**泓君:**
[(48:15)](https://podwise.ai/dashboard/episodes/7501932?locate=2895)
综上我们所有所说的呀，我试着总结一下这个 TPU 跟 GPU 的优势跟劣势。就是整体来看，现在 TPU 它在性能上，包括在模型的训练上，就如果你用得好的话，你是可以把它跑满的，它可能会达到这个 GPU 的性能，甚至是比它就是跑得更好。然后在数据中心的部署上它是更省成本的。这个是它的一些核心优势。同时就是用了它你的推理成本可以降低。那它的缺点就是首先我们说在这个软件的生态上，XLA 它还是一个比较难入门的这样的一个核心门槛。它没有英伟达的这个。另外一点就是它在整个的起量上，包括对这个 HBM 它的供应链的控制上还是比较弱的。还有一个核心的问题就是说，如果大家使用了 TPU, 但是你内部没有特别懂的人，它还是一个黑盒子。就是你没有办法用自己的工程师去把它调优，然后把它的性能跑满。然后如果用谷歌云的话，可能只能跑到这个 50% 到 60% 的性能。在这种情况下，它跟 GPU 谁的性能更好，现在其实也是一个很难说的话题。所以整体上我们看到是这样的一个趋势。TPU 最大的核心的问题就是说，未来如果整个模型它在架构跟算法上有升级，它类似于一款专用的 ASIC 芯片，那通用的 GPU 是更有优势的，但是如果你们的算法读对了，你们就是有核心优势的。我的总结准确吗？

**Henry Zhu:**
[(49:47)](https://podwise.ai/dashboard/episodes/7501932?locate=2987)
我的总结非常精确。

**泓君:**
[(49:49)](https://podwise.ai/dashboard/episodes/7501932?locate=2989)
其实我们前面聊了这么多的 TPU, 你要不要跟大家再简单的回溯一下谷歌它是怎么样去发明这个 TPU 的，它的一个历史是什么，然后谁是中间的核心人物。

**Henry Zhu:**
[(50:01)](https://podwise.ai/dashboard/episodes/7501932?locate=3001)
对，我们最开始 TPU 的话主要是针对内部 CNN 追打模型的一个加速器，然后最开始第一代芯片它只是一款 Inference 芯片，因为刚才我也提到 Inference 相比 Train 更好做一些，所以 Inference 相当于是一个 testing chip，作为一个入口，我们先把这个它里面的架构相对比较简单，它就是一个矩阵计算机加内存，没有其他更多的一些模块，然后最开始的初衷就是大家发现我们内部有很多的 online 的 recommendation system, 但它的 inference 都是用 CPU 来做 inference。大家都知道 CPU 是什么样的问题。就是对它的并行效果是非常非常差的。然后那时候也没办法用 GPU, 因为那时候 GPU 它还没有加入它的那个矩阵计算单元，所以它更多是一个做显卡，做一个 pixel。

**泓君:**
[(50:50)](https://podwise.ai/dashboard/episodes/7501932?locate=3050)
是的。

**Henry Zhu:**
[(50:50)](https://podwise.ai/dashboard/episodes/7501932?locate=3050)
对，所以那时候没有更好用的芯片了。所以谷歌说为什么不自己开发一款只做矩阵计算的模型？所以最开始就是最开始初衷应该是 Jeff Ding 和后来图林讲，获得者 David Patterson 也是深度参与了第一代模型的架构，所以设计出来性能非常非常的好，我们有了第二代芯片。第二代芯片它就是一个非常旗舰的做 training，做训练的模型，所以包括我们后来知道的 AlphaGo，包括 Palm，包括 Bart，包括早期的 Early Transformer, 包括后来 Transformer，都是用了这一套架构去做的训练，但那时候有一个比较大的问题，就是那时候我们先有了硬件，但是软件还没有跟上来，对，包括一直在 VRV3V4 的时候，就一直痛点，就可能硬件上理论上我们心里是不错。对，但是具体能部署多少，第一产能跟不上来，第二就是我们软件上那个协同效应还没有产生，所以一直在做这样一个工作。中间我们也提出了这个 system 的一个 level 的一个 TPU port 的一个拓扑的一个网络，这也是奠定了现在 TPU 那么好的性能的一个基础。然后在第四代的时候，我们又针对 recommendation，推荐和 ranking 的一个算法，我们加入了一个 Sparks Core 西数式的计算单元，这也很好地解决谷歌内部的一些推荐的一些 workload, 所以然后 V5,V6 的话就是开始进入大模型时代了，进入 ChatGPT 时代，所以我们那时候就会针对 Transformer 做了很多很多的优化，

[(52:26)](https://podwise.ai/dashboard/episodes/7501932?locate=3146)
然后一直到我们中间还推出了一个 Inference 的版本，就是因为 Inference 的市场，需求非常非常大，直到现在的一期。

**泓君:**
[(52:36)](https://podwise.ai/dashboard/episodes/7501932?locate=3156)
Inference 的版本是从什么时候开始出来的？

**Henry Zhu:**
[(52:39)](https://podwise.ai/dashboard/episodes/7501932?locate=3159)
应该是 V6 吧，我觉得。我觉得那时候在 TPU 组里工作也挺有意思，就是最开始那几年其实市场上没有那么大的需求，相当于那时候还没有大模型，没有 ChatGPT, 所以我们那时候工作还相对比较轻松一点，因为只是针对自家的 workload，所以感觉就是只要满足内部需求就可以了。

**泓君:**
[(53:00)](https://podwise.ai/dashboard/episodes/7501932?locate=3180)
你是从哪一年开始在 Google 的？我是从 18 年吧。18 年到 24。24,24。哇，那你基本上是完整的参与了好多代的。TPU 的开发。

**Henry Zhu:**
[(53:13)](https://podwise.ai/dashboard/episodes/7501932?locate=3193)
最开始我做的是另外一个 project，但我是从第四代的时候加入，第五代的时候。

**泓君:**
[(53:18)](https://podwise.ai/dashboard/episodes/7501932?locate=3198)
那核心的几代你基本上都有参与。

**Henry Zhu:**
[(53:20)](https://podwise.ai/dashboard/episodes/7501932?locate=3200)
对对对，我不能说是核心几代，我只能说就是核心几代，核心的肯定是第一代和第二代，因为他们是电机者，但后来我是可能经历了从模型快速迭代那个周期，就是你可以想象得出就是最开始大家都在做训练，但一下子哦，现在推理市场一下子变得那么火，那我们要不然做另外一个软本的芯片。Project 一直在是一个非常 volatile，变化非常快的一个状态下。

**泓君:**
[(53:52)](https://podwise.ai/dashboard/episodes/7501932?locate=3232)
当时 Jeff Dean 他是谷歌的首席科学家嘛，然后他其实是在一次整个内部的演示中去演示，说深度学习网络它会怎么样去在语音识别上有一个突破性的进展。就是 2013 年左右的时候，然后那个时候大家其实就发现我们需要的是 GPU 而不是 CPU, 然后后来呢就他们就开始在内部去 demo 这个事情，就是 Jonathan Rose 现在应该也是 Grok 的一个，他应该是 co-founder。

**Henry Zhu:**
[(54:20)](https://podwise.ai/dashboard/episodes/7501932?locate=3260)
现在已经是英伟达的 VP 了。

**泓君:**
[(54:22)](https://podwise.ai/dashboard/episodes/7501932?locate=3262)
对，现在是英伟达的 VP，因为英伟达收购了 Grok。对，他当时就给内部 demo 的时候，他就放了两页 PPT。第一页是这个 GPU 它真的是工作了，这是好消息。然后第二页就是这是一个坏消息，我们付不起这个钱。他们当时是算了说成本，如果所有的用户他们给谷歌发三分钟的语音的话，那么当时谷歌的整个的数据中心的成本会增加一倍，大概是数百亿美元，是非常大的一个量。这个其实也是他们开始去研究 TPU 的一个起点。接下来的故事好像就是到了整个 15,16 年，就是那个 AlphaGo 击败对围棋冠军李世史的时候， 这个时候其实谷歌已经在用 TPU 了，而且据说在他们的 AI 算法里面是放了 4 张 TPU 进去的。那个应该是第一代第二代对不对？

**Henry Zhu:**
[(55:19)](https://podwise.ai/dashboard/episodes/7501932?locate=3319)
应该第二代或第三代吧。

**泓君:**
[(55:21)](https://podwise.ai/dashboard/episodes/7501932?locate=3321)
第二代跟第三代。

**Henry Zhu:**
[(55:23)](https://podwise.ai/dashboard/episodes/7501932?locate=3323)
第一代是不做 training 的。

**泓君:**
[(55:25)](https://podwise.ai/dashboard/episodes/7501932?locate=3325)
对，那比如说刚刚你提到了，其实可能整个大模型出来以后，它是从这个第六代开始的。而且前面它有一些是服务于谷歌的整个的搜索跟广告推荐的这些算法，那第五代到第六代它相当于是一个巨大的方向上的转变。

**Henry Zhu:**
[(55:43)](https://podwise.ai/dashboard/episodes/7501932?locate=3343)
对，我觉得方向上转变更多，就是相当于你可以把重心根据 LLM 现实中的 workload 去做一些调配。就其实理论上来讲，就是 high level 上来讲，就是你把你的计算资源需要变得更大。

**泓君:**
[(56:00)](https://podwise.ai/dashboard/episodes/7501932?locate=3360)
是。

**Henry Zhu:**
[(56:01)](https://podwise.ai/dashboard/episodes/7501932?locate=3361)
对，因为之前可能没有这样的 case，没有这样的 workcase。

**泓君:**
[(56:05)](https://podwise.ai/dashboard/episodes/7501932?locate=3365)
对，软件团队是什么时候加入的？

**Henry Zhu:**
[(56:08)](https://podwise.ai/dashboard/episodes/7501932?locate=3368)
你说 XLA 那边吗？

**泓君:**
[(56:09)](https://podwise.ai/dashboard/episodes/7501932?locate=3369)
对。

**Henry Zhu:**
[(56:10)](https://podwise.ai/dashboard/episodes/7501932?locate=3370)
他们从一开始就有这样的团队在。

**泓君:**
[(56:12)](https://podwise.ai/dashboard/episodes/7501932?locate=3372)
然后谁是这个 TPU 组的核心跟灵魂人物？

**Henry Zhu:**
[(56:20)](https://podwise.ai/dashboard/episodes/7501932?locate=3380)
对，我觉得现在软件团队的规模已经大超过硬件的规模了，因为软件组确实现在目前就生态中扮演一个非常重要的位置，就是它不仅需要跟内部的团队打交道，也需要跟外部的人打交道，所以我觉得现在软件组的话语权会更加重要一些。

**泓君:**
[(56:41)](https://podwise.ai/dashboard/episodes/7501932?locate=3401)
你觉得软件更大众一些，那灵魂人物呢？就比如说 Jeff Dean 在推动这件事情，他肯定是在支持跟推动，早期推动，后期呢？

**Henry Zhu:**
[(56:52)](https://podwise.ai/dashboard/episodes/7501932?locate=3412)
后期的话，后期我觉得可能，就我觉得现在肯定是 Jeff Dean, 包括 DeepMind 那边是一个深度的一个，你可以理解成一个大脑吧，他决定了下一代 TPU 往哪个方向去发展，对，他们决定方向，具体的落实是，落到硬件组。

**泓君:**
[(57:11)](https://podwise.ai/dashboard/episodes/7501932?locate=3431)
嗯，就是落实是硬件组。

**Henry Zhu:**
[(57:13)](https://podwise.ai/dashboard/episodes/7501932?locate=3433)
对，硬件组。

**泓君:**
[(57:14)](https://podwise.ai/dashboard/episodes/7501932?locate=3434)
那我们刚刚其实提到了 Jonathan Rose，我们要不要讲一下 Grok, 因为英伟达其实也是把他收购了。然后他在推理芯片端，我记得他最开始出来的时候，他的整个性能表现啊，然后包括他说的那些方案在业界还是让很多人动心的。

**Henry Zhu:**
[(57:29)](https://podwise.ai/dashboard/episodes/7501932?locate=3449)
对对对。我觉得我当初也跟他们内部团队的人聊过嘛，然后目前他们被英伟达收购是踩准了每一个时代的红利，踩准了每一个很好的时间点。第一个时间点就是 Inference，然后第二个时间点是 ASIC, 第三个时间点就是今年是 Agen 的元年嘛，相当于有很多智能体的爆发。然后 Grok 最好的一个应用 case 就是 Agent 智能体。因为 Agent 智能体的话，它对延迟的要求是非常非常高的。如果说你的延迟做得很差的话，你整个帮你去做这一个任务的一个链条就会被拉到无限长。这是对于单用户来讲的话是一个非常痛苦的一件事情。所以 Grok 就是能很好地去解决或者说解决这样的一个问题。Grok 的芯片，因为 Johnson Ross 最开始是 TPU 的 compiler team, 它是 TPU compiler 那边的一个 Lithuania founder, 这相当于它是带了一套它非常成熟的 TPU 的 compiler XLA 的一些经验去扣放了 Grok, 所以 Grok 你可以理解成它是一家 compiler 的公司而不是一家芯片公司，因为它的芯片更多是为它的软件 compiler 服务的。它的硬件可能比 TPU 更加的单一或者说没那么的智能一些。它的 compiler 在某种程度上决定了一切。它 LPU 里面每一个时间点，每一个计算单元里面具体做哪些事情，它可以精准到每一个 cycle，都是用 compiler 去确定好的一个非常确定性非常高的一个事情。

**泓君:**
[(58:58)](https://podwise.ai/dashboard/episodes/7501932?locate=3538)
你刚刚提到他们精准地踩到了每一个时代的红利点，第一轮是 inference，第二轮是 ASIC，第三轮是 Agent, 这三个时间点对芯片的要求有什么不一样吗？

**Henry Zhu:**
[(59:12)](https://podwise.ai/dashboard/episodes/7501932?locate=3552)
Inference 的话就是因为 Grok 最开始他就主做 Inference，他不做 Training, 他的软件和硬件的架构决定了他做不了 Training, 第二个 ASIC 的话就相当于 ASIC 它的成本更加可控一些。

**泓君:**
[(59:27)](https://podwise.ai/dashboard/episodes/7501932?locate=3567)
它是针对哪个方向的 ASIC?

**Henry Zhu:**
[(59:29)](https://podwise.ai/dashboard/episodes/7501932?locate=3569)
它是针对低延迟的 ASIC，主要是做低延迟，所以现在 agent 它，我可以理解，比如说现在我们用 agent 觉得延迟很低了，它还是有硬件层面的提升的，对，这个是我刚才讨论的就是那个吞吐量的问题，谷歌的它的 TCU 比较好的原因，就是你有很多海量的客户同时去用这样一个 influence 服务。它的存储量就会很高，但它不在乎它的尾部延迟，尾部延迟指的就是你单用户用的话，它可能会有时候会快一点，有时候会慢一点，相信大家也都会有这样的用户体验，但是 Grok，你一旦去用的话，它就会非常非常快，它的原理就是因为它第一是它的 SRAM, 第二就是它是一个，相当于你一个人它占用了非常非常多的 LPU 的资源，而不是跟很多人去共享，它就是这样一套逻辑。

**泓君:**
[(1:00:20)](https://podwise.ai/dashboard/episodes/7501932?locate=3620)
Agent 跟 ASIC 的区别又是什么？我们再讲，顺道第三个逻辑。

**Henry Zhu:**
[(1:00:25)](https://podwise.ai/dashboard/episodes/7501932?locate=3625)
Agent 当然跟 ASIC 没有很多的关联，但 Agent 主要就是 Groov 非常适合 Groov 服务的一个场景。

**泓君:**
[(1:00:31)](https://podwise.ai/dashboard/episodes/7501932?locate=3631)
这是它服务的一些客户。

**Henry Zhu:**
[(1:00:33)](https://podwise.ai/dashboard/episodes/7501932?locate=3633)
对，适合 Groov 服务的场景，第一个是 Agent，第二个是 real time 的一些 voice, 然后还有一些高频的一些交易，它对颜值要求会更高一些。

**泓君:**
[(1:00:44)](https://podwise.ai/dashboard/episodes/7501932?locate=3644)
所以现在 Grock 它的客户主要是以哪些客户为主？比如说像你刚刚提到的高频交易商，然后还有直播，对吧？或者说跟大模型相关的这种实时的直播。

**Henry Zhu:**
[(1:00:56)](https://podwise.ai/dashboard/episodes/7501932?locate=3656)
大家可以去 Google 一下 Grock 这一年它跟哪些厂家签了订单，一个它跟中东的一个数据中心，然后另外它也跟 IBM, 它相当于是一个当地部署的一个集团集群。他只针对本地人去用，相当于在你家搭了一个局域网的感觉，一个私有的语音，然后你用起来会非常非常心理非常非常高。

**泓君:**
[(1:01:24)](https://podwise.ai/dashboard/episodes/7501932?locate=3684)
所以他卖的是什么？就是你刚刚提到他是跟 IBM 签这种数据中心的合同，我开始以为他是卖给 agent 的。就是他是卖芯片或者解决方案给这些 agent 的创业公司的，但是他现在看起来他是在往这个上游去卖。

**Henry Zhu:**
[(1:01:40)](https://podwise.ai/dashboard/episodes/7501932?locate=3700)
具体细节不是非常清楚啊，但我觉得他就是主打一个市场的差异化嘛，就现在主流的市场都是做这种大模大模型的推理和训练，他可能就是。针对一些小的规模部署的一些计算机群做这样一个低延迟的性能优化。

**泓君:**
[(1:01:59)](https://podwise.ai/dashboard/episodes/7501932?locate=3719)
你觉得未来在整个 agent 的应用中，推理芯片会是一个百花齐放的格局，还是说它依然是以英伟达的 GPU 为主导的？

**Henry Zhu:**
[(1:02:10)](https://podwise.ai/dashboard/episodes/7501932?locate=3730)
我觉得自从去年 DeepSeek 出来之后，大家一下子发现就是在推理端就是如果你成本打下来之后的话，端测然后本地的部署，它的推理的需求是非常非常大的。所以这样的话，我觉得就是有很多不同。Layers 的市场和不同 Layers 的那个的指标的需求。当然，谷歌和 TPU 肯定会占据最高层，就是最大规模那些部署的，包括运营啊，包括那些大模型的推理。然后中间和下面的一层的话，我觉得会有更多的玩家，更多的。参与者进来。然后这样的话，相当于 Grok 这块芯片，其实它如果是一个非常大参数量的模型的话，它其实成本是挺高的，我觉得。对，如果说是一个小规模部署的话， 它成本是可以控制的。

**泓君:**
[(1:02:59)](https://podwise.ai/dashboard/episodes/7501932?locate=3779)
所以就是它未来就是整个芯片市场在推理端也会分层， 然后分应用场景。对对对，可能最大量的需求还是这些大的巨头。

**Henry Zhu:**
[(1:03:08)](https://podwise.ai/dashboard/episodes/7501932?locate=3788)
可能就是相当于你要是做 startup 创业公司的话，你没办法说我要做下一家英伟达，你基本上做不到，因为它已经护城河实在太深太深了，你只能做一些尾端的一些客户。很多人都在讨论 TPU 和 GPU 之间区别，我数有数列，但我觉得未来肯定是两者并存的，包括现在所有很多大厂都在自研自己家的芯片，不管是推理还是训练，我觉得你不能说 TPU 将来会一统江山，我觉得这个生态是一个非常健康的生态，有定制的环节，也有通用的环节，有通用的场景，也有定制的场景，也有锤类的场景，所以我觉得将来是百花齐放的一个格局。对，所以一旦产能各方面供应链都解决问题之后，我觉得对用户来讲都是一件好事情。我觉得对一下子把成本下来之后的话，你能做的事情就有无限的可能。

**泓君:**
[(1:04:05)](https://podwise.ai/dashboard/episodes/7501932?locate=3845)
大家都需要有一个人能出来打破垄断。

**Henry Zhu:**
[(1:04:08)](https://podwise.ai/dashboard/episodes/7501932?locate=3848)
对对对，我觉得目前 TPU 可能是一个很好的一个挑战者。是是是。对对，然后我们看一下它这个生态到底能布局到什么样的一个阶段吧。如果它能把一个软件生态也像 CUDA 一样搭起来的话，我觉得对 CUDA 和 GPU 也是一件好事情，也能，我觉得需要同行的激励和鞭策。

**泓君:**
[(1:04:27)](https://podwise.ai/dashboard/episodes/7501932?locate=3867)
而且我觉得其实今天这个话题可能有很多方的人都会关注，比如说从华尔街的视角，他们会非常关注未来整个谷歌的 TPU, 它能抢走这个英伟达多少的市场份额。这绝对是他们就是来去计算估值的这样的一个非常非常重要的点。同时我觉得对很多的这种大模型厂商来说，其实像不仅仅是我们刚刚提到的 Anthropic 跟 Meta, 我看像 XAI 甚至是 OpenAI 都表达出说未来可能会跟谷歌去进行 TPU 的合作。我觉得从模型厂商，这可能也是他们怎么样去提高自己的模型的能力，同时降低自己数据中心的成本的一个非常重要的决策。那我们再往下就是细到这个 TPU 它的整个的供应链环节，包括我们刚刚说到的这个 HBM，包括夜冷。然后包括数据中心它的布局有什么不同。我觉得谷歌它的 TPU 的位置，未来会影响到很多的这个产业链中的各个小的环节跟厂商。对，所以还是很感谢 Henry 今天来我们的这样一期播客，跟我们聊了很多的这个技术细节。我觉得对大家这个做决策会有很多的参考价值。

**Henry Zhu:**
[(1:05:37)](https://podwise.ai/dashboard/episodes/7501932?locate=3937)
不够成投资金。

**泓君:**
[(1:05:38)](https://podwise.ai/dashboard/episodes/7501932?locate=3938)
对对对，那必须做一个声明啊，就是今天我们所有的观点应该都是你的个人言论，不代表公司观点，同时我们所有的这些言论也不构成任何的投资建议。

**Henry Zhu:**
[(1:05:51)](https://podwise.ai/dashboard/episodes/7501932?locate=3951)
对。对，最后补充一点就是因为确实很多 TPU 的布局都是发展在高层，就是他具体想把 TPU 打造成怎样的一款产品，我觉得相当于底下工程师是了解是比较少的，所以我觉得我一直从我在 Google 的工作经历当中去，包括和同事聊天的过程当中去了解一些只言片语吧。然后把它拼凑成一篇故事。

**泓君:**
[(1:06:17)](https://podwise.ai/dashboard/episodes/7501932?locate=3977)
好的，谢谢 Henry。谢谢。

**Henry Zhu:**
[(1:06:19)](https://podwise.ai/dashboard/episodes/7501932?locate=3979)
谢谢。

**泓君:**
[(1:06:19)](https://podwise.ai/dashboard/episodes/7501932?locate=3979)
好，那谢谢 Henry，感谢大家收听我们这一期的播客。如果大家对 TPU 有什么样的想法，可以给我们写评论，写留言。如果大家对我们的播客感兴趣，可以通过苹果播客，Spotify，还有小宇宙来关注我们。如果大家喜欢我们这种视频播客的形式，也可以在 B 站还有 YouTube 上搜索硅谷 101 播客关注我们。我是泓君，感谢大家的收听。

